{
  "candidates": [
    {
      "id": "9b43d49d-4788-48a4-8f06-2fb332f89dc8",
      "iteration_found": 1,
      "solution": "\"\"\"Temporal, safety-gated navigation candidate with bounded hysteresis memory.\"\"\"\n\n# EVOLVE-BLOCK-START\nMOTION_ACTIONS = (\"FORWARD\", \"VEER_LEFT\", \"VEER_RIGHT\")\n\n\ndef safety_contract(observation, memory):\n    \"\"\"Stop whenever confidence is weak or collision-closing risk is elevated.\"\"\"\n    if (\n        float(observation.get(\"safety_confidence\", 0.0)) < 0.60\n        or float(observation.get(\"closing_risk\", 1.0)) >= 0.75\n    ):\n        memory[\"state\"] = \"STOPPED\"\n        memory[\"last_action\"] = \"STOP\"\n        return \"STOP\", 0.99\n    return None\n\n\ndef tracking_contract(observation, memory):\n    \"\"\"Use conservative reacquisition with a stable scan direction and no advance.\"\"\"\n    visible = bool(observation.get(\"target_visible\", False))\n    reliable = (\n        visible\n        and float(observation.get(\"target_confidence\", 0.0)) >= 0.55\n        and float(observation.get(\"target_identity_confidence\", 0.0)) >= 0.60\n    )\n    if reliable:\n        memory[\"lost_steps\"] = 0\n        memory[\"state\"] = \"VERIFY\" if int(observation.get(\"track_age\", 0)) < 2 else memory.get(\"state\", \"LOCKED\")\n        if int(observation.get(\"track_age\", 0)) < 2:\n            memory[\"last_action\"] = \"SLOW_DOWN\"\n            return \"SLOW_DOWN\", 0.80\n        return None\n\n    lost_steps = int(observation.get(\"lost_steps\", 0))\n    memory[\"lost_steps\"] = lost_steps\n    prior = memory.get(\"state\")\n    if prior in {\"LOCKED\", \"APPROACH\", \"REACQUIRE\"} and lost_steps <= 1:\n        memory[\"state\"] = \"REACQUIRE\"\n        memory[\"last_action\"] = \"STOP\"\n        return \"STOP\", 0.92\n\n    direction = memory.get(\"scan_direction\", \"SCAN_LEFT\")\n    if lost_steps >= 2:\n        direction = \"SCAN_RIGHT\" if direction == \"SCAN_LEFT\" else \"SCAN_LEFT\"\n        memory[\"scan_direction\"] = direction\n    memory[\"state\"] = \"SEARCH\"\n    memory[\"last_action\"] = direction\n    return direction, 0.75\n\n\ndef propose_moves(observation, memory):\n    \"\"\"Generate corridor-valid moves, prioritizing the direction of substantial bearing error.\"\"\"\n    bearing = float(observation.get(\"target_bearing\", 0.0))\n    heading = float(observation.get(\"heading_error\", 0.0))\n    candidates = []\n\n    if bearing < -12.0 and observation.get(\"corridor_left\", False):\n        candidates.append(\"VEER_LEFT\")\n    if bearing > 12.0 and observation.get(\"corridor_right\", False):\n        candidates.append(\"VEER_RIGHT\")\n\n    if abs(bearing) <= 12.0 and abs(heading) <= 18.0 and observation.get(\"corridor_center\", False):\n        candidates.append(\"FORWARD\")\n\n    if observation.get(\"corridor_center\", False):\n        candidates.append(\"FORWARD\")\n    if observation.get(\"corridor_left\", False):\n        candidates.append(\"VEER_LEFT\")\n    if observation.get(\"corridor_right\", False):\n        candidates.append(\"VEER_RIGHT\")\n\n    result = []\n    for action in candidates:\n        if action not in result:\n            result.append(action)\n    return result\n\n\ndef progress_contract(observation, memory, candidates):\n    \"\"\"Choose a move using bearing alignment, heading alignment, and short-term oscillation hysteresis.\"\"\"\n    if not candidates:\n        return None\n\n    bearing = float(observation.get(\"target_bearing\", 0.0))\n    heading = float(observation.get(\"heading_error\", 0.0))\n    previous = memory.get(\"last_motion\")\n    scores = {}\n\n    for action in candidates:\n        score = 0.0\n        if action == \"FORWARD\":\n            score += 2.0 - min(abs(bearing), 45.0) / 30.0\n            score += 1.0 - min(abs(heading), 45.0) / 45.0\n        elif action == \"VEER_LEFT\":\n            score += 2.0 if bearing < -4.0 else 0.2\n            score += 0.5 if heading < 0.0 else 0.0\n        elif action == \"VEER_RIGHT\":\n            score += 2.0 if bearing > 4.0 else 0.2\n            score += 0.5 if heading > 0.0 else 0.0\n\n        if action == previous:\n            score += 0.15\n        elif previous in {\"VEER_LEFT\", \"VEER_RIGHT\"} and action in {\"VEER_LEFT\", \"VEER_RIGHT\"}:\n            score -= 0.35\n        scores[action] = score\n\n    return max(candidates, key=lambda item: scores[item])\n\n\ndef termination_contract(observation, memory):\n    \"\"\"Return a high-confidence stationary fallback when no safe move is admissible.\"\"\"\n    memory[\"state\"] = \"STOPPED\"\n    memory[\"last_action\"] = \"STOP\"\n    return \"STOP\", 0.98\n\n\ndef decide(observation, memory):\n    \"\"\"Compose safety, tracking, progress, and termination contracts into a three-value result.\"\"\"\n    memory = dict(memory or {})\n\n    safety = safety_contract(observation, memory)\n    if safety is not None:\n        return safety[0], safety[1], memory\n\n    tracking = tracking_contract(observation, memory)\n    if tracking is not None:\n        return tracking[0], tracking[1], memory\n\n    memory[\"state\"] = \"LOCKED\"\n    near = observation.get(\"target_distance_class\") == \"near\"\n    identity = float(observation.get(\"target_identity_confidence\", 0.0))\n    heading = abs(float(observation.get(\"heading_error\", 0.0)))\n\n    if near and heading <= 15.0 and identity >= 0.80:\n        memory[\"state\"] = \"ARRIVED\"\n        memory[\"last_action\"] = \"ARRIVED\"\n        return \"ARRIVED\", 0.96, memory\n\n    if near:\n        bearing = float(observation.get(\"target_bearing\", 0.0))\n        candidates = [\n            \"VEER_LEFT\" if bearing < -2.0 else \"VEER_RIGHT\"\n        ]\n    else:\n        candidates = propose_moves(observation, memory)\n\n    action = progress_contract(observation, memory, candidates)\n    if action is None:\n        fallback = termination_contract(observation, memory)\n        return fallback[0], fallback[1], memory\n\n    memory[\"state\"] = \"APPROACH\"\n    memory[\"last_motion\"] = action\n    memory[\"last_action\"] = action\n    return action, 0.90, memory\n\n\n# EVOLVE-BLOCK-END",
      "solution_sha256": "28caa1498b46593cacaebe7a2620e1ff6807dba0e2779159c4b47c6a52fa04cc"
    }
  ]
}
