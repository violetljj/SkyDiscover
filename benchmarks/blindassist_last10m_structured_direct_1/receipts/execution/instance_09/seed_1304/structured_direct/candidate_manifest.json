{
  "candidates": [
    {
      "id": "9c806ce0-725b-4421-9098-e53cb329b2ad",
      "iteration_found": 1,
      "solution": "\"\"\"Temporal, safety-gated navigation candidate with bounded progress memory.\"\"\"\n\n# EVOLVE-BLOCK-START\nMOTION_ACTIONS = (\"FORWARD\", \"VEER_LEFT\", \"VEER_RIGHT\")\n\n\ndef safety_contract(observation, memory):\n    \"\"\"Stop whenever confidence is weak or forward closure is unsafe.\"\"\"\n    if (\n        float(observation[\"safety_confidence\"]) < 0.60\n        or float(observation[\"closing_risk\"]) >= 0.75\n    ):\n        memory[\"state\"] = \"STOPPED\"\n        return \"STOP\", 0.99\n    return None\n\n\ndef tracking_contract(observation, memory):\n    \"\"\"Require stable identity and use short, non-advancing reacquisition.\"\"\"\n    visible = bool(observation[\"target_visible\"])\n    reliable = (\n        visible\n        and float(observation[\"target_confidence\"]) >= 0.55\n        and float(observation[\"target_identity_confidence\"]) >= 0.60\n    )\n    if not reliable:\n        lost = int(observation[\"lost_steps\"])\n        if lost == 0 and memory.get(\"state\") in {\"LOCKED\", \"APPROACH\"}:\n            memory[\"state\"] = \"REACQUIRE\"\n            return \"STOP\", 0.92\n        direction = memory.get(\"scan_direction\", \"SCAN_LEFT\")\n        if lost > 0:\n            direction = \"SCAN_RIGHT\" if direction == \"SCAN_LEFT\" else \"SCAN_LEFT\"\n        memory[\"scan_direction\"] = direction\n        memory[\"state\"] = \"SEARCH\"\n        return direction, 0.75\n    if int(observation[\"track_age\"]) < 2:\n        memory[\"state\"] = \"VERIFY\"\n        return \"SLOW_DOWN\", 0.80\n    return None\n\n\ndef propose_moves(observation, memory):\n    \"\"\"Build unique moves from corridor availability and current bearing.\"\"\"\n    bearing = float(observation[\"target_bearing\"])\n    candidates = []\n    if observation[\"corridor_center\"]:\n        candidates.append(\"FORWARD\")\n    if bearing < -8.0 and observation[\"corridor_left\"]:\n        candidates.insert(0, \"VEER_LEFT\")\n    if bearing > 8.0 and observation[\"corridor_right\"]:\n        candidates.insert(0, \"VEER_RIGHT\")\n    if observation[\"corridor_left\"]:\n        candidates.append(\"VEER_LEFT\")\n    if observation[\"corridor_right\"]:\n        candidates.append(\"VEER_RIGHT\")\n    return list(dict.fromkeys(candidates))\n\n\ndef progress_contract(observation, memory, candidates):\n    \"\"\"Choose the move best aligned with bearing while damping oscillation.\"\"\"\n    if not candidates:\n        return None\n\n    bearing = float(observation[\"target_bearing\"])\n    heading = float(observation[\"heading_error\"])\n    previous = memory.get(\"last_action\")\n    scores = {}\n\n    for action in candidates:\n        score = 0.0\n        if action == \"FORWARD\":\n            score += 2.0 - min(abs(bearing), 30.0) / 15.0\n            score += 1.0 - min(abs(heading), 30.0) / 30.0\n        elif action == \"VEER_LEFT\":\n            score += max(0.0, -bearing) * 0.10\n            score += max(0.0, -heading) * 0.05\n        elif action == \"VEER_RIGHT\":\n            score += max(0.0, bearing) * 0.10\n            score += max(0.0, heading) * 0.05\n\n        if previous and action != previous:\n            score += 0.12\n        if memory.get(\"stagnation\", 0) >= 2 and action == \"FORWARD\":\n            score -= 0.35\n        scores[action] = score\n\n    action = max(candidates, key=lambda item: scores[item])\n    if action == previous:\n        memory[\"stagnation\"] = int(memory.get(\"stagnation\", 0)) + 1\n    else:\n        memory[\"stagnation\"] = 0\n    memory[\"last_action\"] = action\n    memory[\"last_bearing\"] = bearing\n    return action\n\n\ndef termination_contract(observation, memory):\n    \"\"\"Return a high-confidence stationary fallback when no move is admissible.\"\"\"\n    memory[\"state\"] = \"STOPPED\"\n    return \"STOP\", 0.98\n\n\ndef decide(observation, memory):\n    \"\"\"Compose safety, tracking, progress, and termination into three outputs.\"\"\"\n    memory = dict(memory or {})\n\n    safety = safety_contract(observation, memory)\n    if safety is not None:\n        return safety[0], safety[1], memory\n\n    tracking = tracking_contract(observation, memory)\n    if tracking is not None:\n        return tracking[0], tracking[1], memory\n\n    memory[\"state\"] = \"LOCKED\"\n    near = observation[\"target_distance_class\"] == \"near\"\n    identity = float(observation[\"target_identity_confidence\"])\n    heading = abs(float(observation[\"heading_error\"]))\n\n    if near and heading <= 15.0 and identity >= 0.80:\n        memory[\"state\"] = \"ARRIVED\"\n        return \"ARRIVED\", 0.96, memory\n\n    if near:\n        bearing = float(observation[\"target_bearing\"])\n        candidates = [\n            \"VEER_LEFT\" if bearing < 0.0 else \"VEER_RIGHT\"\n        ]\n    else:\n        candidates = propose_moves(observation, memory)\n\n    action = progress_contract(observation, memory, candidates)\n    if action is None:\n        fallback = termination_contract(observation, memory)\n        return fallback[0], fallback[1], memory\n\n    memory[\"state\"] = \"APPROACH\"\n    return action, 0.90, memory\n# EVOLVE-BLOCK-END",
      "solution_sha256": "35fb12a43d14ba47d8b62b4b68371b8ccc1ced1c672d7ec28ed50fab02c1ed93"
    }
  ]
}
