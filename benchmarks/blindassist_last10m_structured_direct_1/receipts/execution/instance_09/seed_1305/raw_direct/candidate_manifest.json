{
  "candidates": [
    {
      "id": "b4b9c2c9-ed5a-4315-888b-a62f212df9ff",
      "iteration_found": 1,
      "solution": "# EVOLVE-BLOCK-START\ndef decide(observation, memory):\n    \"\"\"Use safety gating, confidence confirmation, and hysteretic target steering.\"\"\"\n\n    memory = dict(memory or {})\n    state = memory.get(\"state\", \"IDLE\")\n    last_action = memory.get(\"last_action\", \"\")\n    scan_direction = memory.get(\"scan_direction\", \"SCAN_LEFT\")\n\n    visible = bool(observation[\"target_visible\"])\n    bearing = float(observation[\"target_bearing\"])\n    distance = observation[\"target_distance_class\"]\n    target_confidence = float(observation[\"target_confidence\"])\n    identity_confidence = float(observation[\"target_identity_confidence\"])\n    track_age = int(observation[\"track_age\"])\n    lost_steps = int(observation[\"lost_steps\"])\n    safety_confidence = float(observation[\"safety_confidence\"])\n    closing_risk = float(observation[\"closing_risk\"])\n    heading_error = abs(float(observation[\"heading_error\"]))\n\n    def emit(action, confidence, next_state):\n        memory[\"state\"] = next_state\n        memory[\"last_action\"] = action\n        return action, confidence, memory\n\n    # Safety is an unconditional hard gate.\n    if safety_confidence < 0.60 or closing_risk >= 0.75:\n        return emit(\"STOP\", 0.99, \"STOPPED\")\n\n    # Require a stable, identified target before moving.\n    if not visible or target_confidence < 0.55 or identity_confidence < 0.60:\n        if state in {\"LOCKED\", \"APPROACH\", \"VERIFY\", \"REACQUIRE\"} or lost_steps > 0:\n            if lost_steps <= 1:\n                return emit(\"STOP\", 0.92, \"REACQUIRE\")\n            action = scan_direction\n            if lost_steps % 2 == 0:\n                scan_direction = (\n                    \"SCAN_RIGHT\" if scan_direction == \"SCAN_LEFT\" else \"SCAN_LEFT\"\n                )\n            memory[\"scan_direction\"] = scan_direction\n            return emit(action, 0.75, \"SEARCH\")\n        return emit(scan_direction, 0.72, \"SEARCH\")\n\n    if track_age < 2:\n        return emit(\"SLOW_DOWN\", 0.80, \"VERIFY\")\n\n    if distance == \"near\":\n        if heading_error <= 15.0 and identity_confidence >= 0.80:\n            return emit(\"ARRIVED\", 0.96, \"ARRIVED\")\n        if bearing < -2.0 and observation[\"corridor_left\"]:\n            return emit(\"VEER_LEFT\", 0.88, \"APPROACH\")\n        if bearing > 2.0 and observation[\"corridor_right\"]:\n            return emit(\"VEER_RIGHT\", 0.88, \"APPROACH\")\n        if observation[\"corridor_center\"] and heading_error <= 25.0:\n            return emit(\"FORWARD\", 0.86, \"APPROACH\")\n        return emit(\"STOP\", 0.98, \"STOPPED\")\n\n    # Preserve a turn briefly near the threshold to avoid left/right chatter.\n    if last_action == \"VEER_LEFT\" and bearing < 16.0 and observation[\"corridor_left\"]:\n        return emit(\"VEER_LEFT\", 0.86, \"APPROACH\")\n    if last_action == \"VEER_RIGHT\" and bearing > -16.0 and observation[\"corridor_right\"]:\n        return emit(\"VEER_RIGHT\", 0.86, \"APPROACH\")\n\n    if bearing < -12.0:\n        if observation[\"corridor_left\"]:\n            return emit(\"VEER_LEFT\", 0.90, \"APPROACH\")\n        return emit(\"STOP\", 0.98, \"STOPPED\")\n    if bearing > 12.0:\n        if observation[\"corridor_right\"]:\n            return emit(\"VEER_RIGHT\", 0.90, \"APPROACH\")\n        return emit(\"STOP\", 0.98, \"STOPPED\")\n\n    if observation[\"corridor_center\"]:\n        return emit(\"FORWARD\", 0.90, \"APPROACH\")\n    if abs(bearing) <= 12.0 and observation[\"corridor_left\"] and not observation[\"corridor_right\"]:\n        return emit(\"VEER_LEFT\", 0.82, \"APPROACH\")\n    if abs(bearing) <= 12.0 and observation[\"corridor_right\"] and not observation[\"corridor_left\"]:\n        return emit(\"VEER_RIGHT\", 0.82, \"APPROACH\")\n    return emit(\"STOP\", 0.98, \"STOPPED\")\n# EVOLVE-BLOCK-END",
      "solution_sha256": "2d0eb674a1ed7c98c386f5f0d7a2cf7a99a5537c4976e94625d207321a6e043d"
    }
  ]
}
