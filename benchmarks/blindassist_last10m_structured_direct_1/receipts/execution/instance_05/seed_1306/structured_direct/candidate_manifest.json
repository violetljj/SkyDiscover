{
  "candidates": [
    {
      "id": "cfd76296-6c6e-4e8c-8906-1a7979827883",
      "iteration_found": 1,
      "solution": "\"\"\"Temporal candidate controller preserving the six structured contracts.\"\"\"\n\n# EVOLVE-BLOCK-START\nMOTION_ACTIONS = (\"FORWARD\", \"VEER_LEFT\", \"VEER_RIGHT\")\n\n\ndef safety_contract(observation, memory):\n    \"\"\"Stop whenever confidence is low or collision risk is elevated.\"\"\"\n    if (\n        float(observation[\"safety_confidence\"]) < 0.60\n        or float(observation[\"closing_risk\"]) >= 0.75\n    ):\n        memory[\"state\"] = \"STOPPED\"\n        memory[\"last_action\"] = \"STOP\"\n        return \"STOP\", 0.99\n    return None\n\n\ndef tracking_contract(observation, memory):\n    \"\"\"Require stable identity and use bounded, hysteretic reacquisition.\"\"\"\n    visible = bool(observation[\"target_visible\"])\n    reliable = (\n        visible\n        and float(observation[\"target_confidence\"]) >= 0.55\n        and float(observation[\"target_identity_confidence\"]) >= 0.60\n    )\n    if not reliable:\n        lost = int(observation[\"lost_steps\"])\n        previous = memory.get(\"state\")\n        if previous in {\"LOCKED\", \"APPROACH\", \"REACQUIRE\"} and lost <= 1:\n            memory[\"state\"] = \"REACQUIRE\"\n            memory[\"last_action\"] = \"STOP\"\n            return \"STOP\", 0.92\n        direction = memory.get(\"scan_direction\", \"SCAN_LEFT\")\n        if lost >= 2:\n            direction = \"SCAN_RIGHT\" if direction == \"SCAN_LEFT\" else \"SCAN_LEFT\"\n        memory[\"scan_direction\"] = direction\n        memory[\"state\"] = \"SEARCH\"\n        memory[\"last_action\"] = direction\n        return direction, 0.75\n    if int(observation[\"track_age\"]) < 2:\n        memory[\"state\"] = \"VERIFY\"\n        memory[\"last_action\"] = \"SLOW_DOWN\"\n        return \"SLOW_DOWN\", 0.80\n    return None\n\n\ndef propose_moves(observation, memory):\n    \"\"\"Build deduplicated moves ordered by bearing and available corridor.\"\"\"\n    bearing = float(observation[\"target_bearing\"])\n    candidates = []\n    if bearing < -12.0 and observation[\"corridor_left\"]:\n        candidates.append(\"VEER_LEFT\")\n    if bearing > 12.0 and observation[\"corridor_right\"]:\n        candidates.append(\"VEER_RIGHT\")\n    if observation[\"corridor_center\"]:\n        candidates.append(\"FORWARD\")\n    if observation[\"corridor_left\"]:\n        candidates.append(\"VEER_LEFT\")\n    if observation[\"corridor_right\"]:\n        candidates.append(\"VEER_RIGHT\")\n    return list(dict.fromkeys(candidates))\n\n\ndef progress_contract(observation, memory, candidates):\n    \"\"\"Apply action hysteresis while preventing prolonged directional stalling.\"\"\"\n    if not candidates:\n        return None\n\n    previous = memory.get(\"last_motion\")\n    streak = int(memory.get(\"motion_streak\", 0))\n    bearing = float(observation[\"target_bearing\"])\n\n    if previous in candidates and streak < 3:\n        if previous == \"FORWARD\" and abs(bearing) <= 25.0:\n            choice = previous\n        elif previous in {\"VEER_LEFT\", \"VEER_RIGHT\"} and abs(bearing) <= 30.0:\n            choice = previous\n        else:\n            choice = candidates[0]\n    else:\n        choice = candidates[0]\n\n    if (\n        streak >= 3\n        and choice == previous\n        and len(candidates) > 1\n    ):\n        choice = candidates[1]\n\n    if choice == previous:\n        memory[\"motion_streak\"] = streak + 1\n    else:\n        memory[\"motion_streak\"] = 1\n    memory[\"last_motion\"] = choice\n    memory[\"last_action\"] = choice\n    return choice\n\n\ndef termination_contract(observation, memory):\n    \"\"\"Return a high-confidence stationary fallback when motion is unavailable.\"\"\"\n    memory[\"state\"] = \"STOPPED\"\n    memory[\"last_action\"] = \"STOP\"\n    return \"STOP\", 0.98\n\n\ndef decide(observation, memory):\n    \"\"\"Compose safety, tracking, arrival, progress, and termination decisions.\"\"\"\n    memory = dict(memory or {})\n\n    safety = safety_contract(observation, memory)\n    if safety is not None:\n        return safety[0], safety[1], memory\n\n    tracking = tracking_contract(observation, memory)\n    if tracking is not None:\n        return tracking[0], tracking[1], memory\n\n    memory[\"state\"] = \"LOCKED\"\n    near = observation[\"target_distance_class\"] == \"near\"\n    if near:\n        aligned = (\n            abs(float(observation[\"heading_error\"])) <= 15.0\n            and float(observation[\"target_identity_confidence\"]) >= 0.80\n        )\n        if aligned:\n            memory[\"state\"] = \"ARRIVED\"\n            memory[\"last_action\"] = \"ARRIVED\"\n            return \"ARRIVED\", 0.96, memory\n        bearing = float(observation[\"target_bearing\"])\n        candidates = [\n            \"VEER_LEFT\" if bearing < 0.0 else \"VEER_RIGHT\"\n        ]\n    else:\n        candidates = propose_moves(observation, memory)\n\n    action = progress_contract(observation, memory, candidates)\n    if action is None:\n        fallback = termination_contract(observation, memory)\n        return fallback[0], fallback[1], memory\n\n    memory[\"state\"] = \"APPROACH\"\n    return action, 0.90, memory\n\n\n# EVOLVE-BLOCK-END",
      "solution_sha256": "d774090a86aa3b2391155f898299bc0835cabcb133ebb705c4f24eb5b5f5a289"
    }
  ]
}
