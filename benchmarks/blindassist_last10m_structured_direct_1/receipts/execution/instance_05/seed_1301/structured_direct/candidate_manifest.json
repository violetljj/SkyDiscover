{
  "candidates": [
    {
      "id": "7716a5a8-466b-4caf-bf0d-9a116a0ce3bf",
      "iteration_found": 1,
      "solution": "\"\"\"Temporal, safety-gated candidate generator with bounded tracking and progress memory.\"\"\"\n\n# EVOLVE-BLOCK-START\nMOTION_ACTIONS = (\"FORWARD\", \"VEER_LEFT\", \"VEER_RIGHT\")\n\n\ndef safety_contract(observation, memory):\n    \"\"\"Stop whenever confidence is low or collision risk is high.\"\"\"\n    if (\n        float(observation.get(\"safety_confidence\", 0.0)) < 0.60\n        or float(observation.get(\"closing_risk\", 1.0)) >= 0.75\n    ):\n        memory[\"state\"] = \"STOPPED\"\n        return \"STOP\", 0.99\n    return None\n\n\ndef tracking_contract(observation, memory):\n    \"\"\"Use a short stabilizing stop, then bounded alternating reacquisition scans.\"\"\"\n    visible = bool(observation.get(\"target_visible\", False))\n    reliable = (\n        visible\n        and float(observation.get(\"target_confidence\", 0.0)) >= 0.55\n        and float(observation.get(\"target_identity_confidence\", 0.0)) >= 0.60\n    )\n    if reliable:\n        memory[\"lost_steps\"] = 0\n        if int(observation.get(\"track_age\", 0)) < 2:\n            memory[\"state\"] = \"VERIFY\"\n            return \"SLOW_DOWN\", 0.80\n        return None\n\n    lost = int(observation.get(\"lost_steps\", 0))\n    prior = memory.get(\"state\")\n    if prior in {\"LOCKED\", \"APPROACH\", \"REACQUIRE\", \"VERIFY\"} and lost <= 1:\n        memory[\"state\"] = \"REACQUIRE\"\n        memory[\"lost_steps\"] = lost\n        return \"STOP\", 0.92\n\n    direction = memory.get(\"scan_direction\", \"SCAN_LEFT\")\n    memory[\"scan_direction\"] = (\n        \"SCAN_RIGHT\" if direction == \"SCAN_LEFT\" else \"SCAN_LEFT\"\n    )\n    memory[\"state\"] = \"SEARCH\"\n    memory[\"lost_steps\"] = lost\n    return memory[\"scan_direction\"], 0.75\n\n\ndef propose_moves(observation, memory):\n    \"\"\"Build de-duplicated moves ordered by the observed bearing and open corridors.\"\"\"\n    bearing = float(observation.get(\"target_bearing\", 0.0))\n    candidates = []\n    if bearing < -12.0 and observation.get(\"corridor_left\", False):\n        candidates.append(\"VEER_LEFT\")\n    if bearing > 12.0 and observation.get(\"corridor_right\", False):\n        candidates.append(\"VEER_RIGHT\")\n    if observation.get(\"corridor_center\", False):\n        candidates.append(\"FORWARD\")\n    if observation.get(\"corridor_left\", False):\n        candidates.append(\"VEER_LEFT\")\n    if observation.get(\"corridor_right\", False):\n        candidates.append(\"VEER_RIGHT\")\n    return list(dict.fromkeys(candidates))\n\n\ndef progress_contract(observation, memory, candidates):\n    \"\"\"Prefer bearing-correcting moves and escape repeated actions after stalled progress.\"\"\"\n    if not candidates:\n        return None\n\n    bearing = float(observation.get(\"target_bearing\", 0.0))\n    previous = memory.get(\"last_action\")\n    current_distance = observation.get(\"target_distance\")\n    previous_distance = memory.get(\"last_distance\")\n\n    if isinstance(current_distance, (int, float)):\n        if isinstance(previous_distance, (int, float)) and current_distance >= previous_distance:\n            memory[\"stall_count\"] = int(memory.get(\"stall_count\", 0)) + 1\n        else:\n            memory[\"stall_count\"] = 0\n        memory[\"last_distance\"] = current_distance\n\n    stall = int(memory.get(\"stall_count\", 0))\n    ranked = sorted(\n        candidates,\n        key=lambda action: (\n            0 if (bearing < -12 and action == \"VEER_LEFT\") else\n            0 if (bearing > 12 and action == \"VEER_RIGHT\") else\n            1 if action == \"FORWARD\" else 2,\n            1 if action == previous and stall >= 2 else 0,\n        ),\n    )\n    action = ranked[0]\n    memory[\"last_action\"] = action\n    return action\n\n\ndef termination_contract(observation, memory):\n    \"\"\"Return a high-confidence stationary fallback when no move is admissible.\"\"\"\n    memory[\"state\"] = \"STOPPED\"\n    return \"STOP\", 0.98\n\n\ndef decide(observation, memory):\n    \"\"\"Compose safety, tracking, arrival, progress, and termination into three outputs.\"\"\"\n    memory = dict(memory or {})\n\n    safety = safety_contract(observation, memory)\n    if safety is not None:\n        return safety[0], safety[1], memory\n\n    tracking = tracking_contract(observation, memory)\n    if tracking is not None:\n        return tracking[0], tracking[1], memory\n\n    memory[\"state\"] = \"LOCKED\"\n    near = observation.get(\"target_distance_class\") == \"near\"\n    identity = float(observation.get(\"target_identity_confidence\", 0.0))\n    heading = abs(float(observation.get(\"heading_error\", 0.0)))\n\n    if near and heading <= 12.0 and identity >= 0.80:\n        memory[\"state\"] = \"ARRIVED\"\n        return \"ARRIVED\", 0.96, memory\n\n    if near:\n        bearing = float(observation.get(\"target_bearing\", 0.0))\n        candidates = [\"VEER_LEFT\" if bearing < 0.0 else \"VEER_RIGHT\"]\n    else:\n        candidates = propose_moves(observation, memory)\n\n    action = progress_contract(observation, memory, candidates)\n    if action is None:\n        fallback = termination_contract(observation, memory)\n        return fallback[0], fallback[1], memory\n\n    memory[\"state\"] = \"APPROACH\"\n    return action, 0.90, memory\n\n\n# EVOLVE-BLOCK-END",
      "solution_sha256": "457686b13a80c646ba2e089126c3a501d95493878e14453a490997b4c6984965"
    }
  ]
}
