{
  "candidates": [
    {
      "id": "c97d10bb-0f70-4ca8-be7b-65ce39693e88",
      "iteration_found": 1,
      "solution": "\"\"\"Temporal candidate controller with safety gating, target verification, and\nbounded progress memory for stable, efficient navigation.\"\"\"\n\n# EVOLVE-BLOCK-START\nMOTION_ACTIONS = (\"FORWARD\", \"VEER_LEFT\", \"VEER_RIGHT\")\n\n\ndef safety_contract(observation, memory):\n    \"\"\"Stop immediately when confidence is low or collision risk is elevated.\"\"\"\n    if (\n        float(observation.get(\"safety_confidence\", 0.0)) < 0.60\n        or float(observation.get(\"closing_risk\", 1.0)) >= 0.75\n    ):\n        memory[\"state\"] = \"STOPPED\"\n        return \"STOP\", 0.99\n    return None\n\n\ndef tracking_contract(observation, memory):\n    \"\"\"Verify target quality and reacquire conservatively after brief loss.\"\"\"\n    visible = bool(observation.get(\"target_visible\", False))\n    reliable = (\n        visible\n        and float(observation.get(\"target_confidence\", 0.0)) >= 0.55\n        and float(observation.get(\"target_identity_confidence\", 0.0)) >= 0.60\n    )\n    if reliable:\n        if int(observation.get(\"track_age\", 0)) < 2:\n            memory[\"state\"] = \"VERIFY\"\n            return \"SLOW_DOWN\", 0.80\n        return None\n\n    lost_steps = int(observation.get(\"lost_steps\", 99))\n    if memory.get(\"state\") in {\"LOCKED\", \"APPROACH\", \"REACQUIRE\"} and lost_steps <= 1:\n        memory[\"state\"] = \"REACQUIRE\"\n        return \"STOP\", 0.92\n\n    direction = memory.get(\"scan_direction\", \"SCAN_LEFT\")\n    memory[\"scan_direction\"] = \"SCAN_RIGHT\" if direction == \"SCAN_LEFT\" else \"SCAN_LEFT\"\n    memory[\"state\"] = \"SEARCH\"\n    return direction, 0.75\n\n\ndef propose_moves(observation, memory):\n    \"\"\"Generate deduplicated actions with a centered-forward preference.\"\"\"\n    bearing = float(observation.get(\"target_bearing\", 0.0))\n    left = bool(observation.get(\"corridor_left\", False))\n    right = bool(observation.get(\"corridor_right\", False))\n    center = bool(observation.get(\"corridor_center\", False))\n    candidates = []\n\n    if abs(bearing) <= 12.0 and center:\n        candidates.append(\"FORWARD\")\n    elif bearing < -12.0 and left:\n        candidates.append(\"VEER_LEFT\")\n    elif bearing > 12.0 and right:\n        candidates.append(\"VEER_RIGHT\")\n\n    if center:\n        candidates.append(\"FORWARD\")\n    if left:\n        candidates.append(\"VEER_LEFT\")\n    if right:\n        candidates.append(\"VEER_RIGHT\")\n    return list(dict.fromkeys(candidates))\n\n\ndef progress_contract(observation, memory, candidates):\n    \"\"\"Use bounded temporal memory to avoid oscillation and retry stalled moves.\"\"\"\n    if not candidates:\n        return None\n\n    distance = float(observation.get(\"target_distance\", 0.0))\n    previous_distance = memory.get(\"previous_distance\")\n    if previous_distance is not None:\n        if distance < previous_distance - 0.01:\n            memory[\"stagnation\"] = 0\n        else:\n            memory[\"stagnation\"] = min(int(memory.get(\"stagnation\", 0)) + 1, 3)\n    memory[\"previous_distance\"] = distance\n\n    last = memory.get(\"last_action\")\n    stagnation = int(memory.get(\"stagnation\", 0))\n\n    if last in candidates and stagnation < 2:\n        choice = last\n    else:\n        choice = candidates[0]\n        if stagnation >= 2:\n            for candidate in candidates:\n                if candidate != last:\n                    choice = candidate\n                    break\n\n    memory[\"last_action\"] = choice\n    return choice\n\n\ndef termination_contract(observation, memory):\n    \"\"\"Return a high-confidence stationary fallback when no motion is admissible.\"\"\"\n    memory[\"state\"] = \"STOPPED\"\n    return \"STOP\", 0.98\n\n\ndef decide(observation, memory):\n    \"\"\"Compose safety, tracking, arrival, progress, and termination contracts.\"\"\"\n    memory = dict(memory or {})\n\n    safety = safety_contract(observation, memory)\n    if safety is not None:\n        return safety[0], safety[1], memory\n\n    tracking = tracking_contract(observation, memory)\n    if tracking is not None:\n        return tracking[0], tracking[1], memory\n\n    memory[\"state\"] = \"LOCKED\"\n    near = observation.get(\"target_distance_class\") == \"near\"\n    identity = float(observation.get(\"target_identity_confidence\", 0.0))\n    heading = abs(float(observation.get(\"heading_error\", 0.0)))\n\n    if near and heading <= 15.0 and identity >= 0.80:\n        memory[\"state\"] = \"ARRIVED\"\n        return \"ARRIVED\", 0.96, memory\n\n    if near:\n        bearing = float(observation.get(\"target_bearing\", 0.0))\n        candidates = [\"VEER_LEFT\" if bearing < 0.0 else \"VEER_RIGHT\"]\n    else:\n        candidates = propose_moves(observation, memory)\n\n    action = progress_contract(observation, memory, candidates)\n    if action is None:\n        fallback = termination_contract(observation, memory)\n        return fallback[0], fallback[1], memory\n\n    memory[\"state\"] = \"APPROACH\"\n    return action, 0.90, memory\n\n\n# EVOLVE-BLOCK-END",
      "solution_sha256": "18f57269c8c3c14c4bb694c621ef591d696b62144941491dc34e0d5a8d14638c"
    }
  ]
}
