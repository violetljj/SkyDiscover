{
  "candidates": [
    {
      "id": "6dbebac0-434a-4555-94c3-e444b80efb7e",
      "iteration_found": 1,
      "solution": "\"\"\"Temporal, safety-first candidate controller with hysteretic steering.\"\"\"\n\n# EVOLVE-BLOCK-START\nMOTION_ACTIONS = (\"FORWARD\", \"VEER_LEFT\", \"VEER_RIGHT\")\n\n\ndef safety_contract(observation, memory):\n    \"\"\"Stop whenever safety confidence is low or closing risk is excessive.\"\"\"\n    if (\n        float(observation[\"safety_confidence\"]) < 0.60\n        or float(observation[\"closing_risk\"]) >= 0.75\n    ):\n        memory[\"state\"] = \"STOPPED\"\n        return \"STOP\", 0.99\n    return None\n\n\ndef tracking_contract(observation, memory):\n    \"\"\"Require a stable target and use bounded, non-advancing reacquisition.\"\"\"\n    reliable = (\n        bool(observation[\"target_visible\"])\n        and float(observation[\"target_confidence\"]) >= 0.55\n        and float(observation[\"target_identity_confidence\"]) >= 0.60\n    )\n    if reliable:\n        if int(observation[\"track_age\"]) < 2:\n            memory[\"state\"] = \"VERIFY\"\n            return \"SLOW_DOWN\", 0.80\n        return None\n\n    lost_steps = int(observation[\"lost_steps\"])\n    if memory.get(\"state\") in {\"LOCKED\", \"APPROACH\", \"REACQUIRE\"} and lost_steps <= 1:\n        memory[\"state\"] = \"REACQUIRE\"\n        return \"STOP\", 0.92\n\n    direction = memory.get(\"scan_direction\", \"SCAN_LEFT\")\n    memory[\"scan_direction\"] = (\n        \"SCAN_RIGHT\" if direction == \"SCAN_LEFT\" else \"SCAN_LEFT\"\n    )\n    memory[\"state\"] = \"SEARCH\"\n    return direction, 0.75\n\n\ndef propose_moves(observation, memory):\n    \"\"\"Build deduplicated moves, preferring the side that reduces bearing error.\"\"\"\n    bearing = float(observation[\"target_bearing\"])\n    candidates = []\n\n    if abs(bearing) <= 10.0 and observation[\"corridor_center\"]:\n        candidates.append(\"FORWARD\")\n    elif bearing < -10.0 and observation[\"corridor_left\"]:\n        candidates.append(\"VEER_LEFT\")\n    elif bearing > 10.0 and observation[\"corridor_right\"]:\n        candidates.append(\"VEER_RIGHT\")\n\n    if observation[\"corridor_center\"]:\n        candidates.append(\"FORWARD\")\n    if observation[\"corridor_left\"]:\n        candidates.append(\"VEER_LEFT\")\n    if observation[\"corridor_right\"]:\n        candidates.append(\"VEER_RIGHT\")\n\n    return list(dict.fromkeys(candidates))\n\n\ndef progress_contract(observation, memory, candidates):\n    \"\"\"Apply bearing-based hysteresis to avoid zig-zagging and needless turns.\"\"\"\n    if not candidates:\n        return None\n\n    bearing = float(observation[\"target_bearing\"])\n    preferred = (\n        \"VEER_LEFT\" if bearing < -10.0\n        else \"VEER_RIGHT\" if bearing > 10.0\n        else \"FORWARD\"\n    )\n    last = memory.get(\"last_action\")\n    scores = {}\n\n    for action in candidates:\n        score = 0.0\n        if action == preferred:\n            score += 4.0\n        if action == \"FORWARD\":\n            score += 1.5 if abs(bearing) <= 18.0 else 0.0\n        if last in {\"VEER_LEFT\", \"VEER_RIGHT\"}:\n            if action != last and action in {\"VEER_LEFT\", \"VEER_RIGHT\"}:\n                score -= 1.5\n            if action == \"FORWARD\" and abs(bearing) <= 22.0:\n                score += 1.0\n        scores[action] = score\n\n    action = max(candidates, key=lambda item: scores[item])\n    history = list(memory.get(\"action_history\", []))\n    history.append(action)\n    memory[\"action_history\"] = history[-4:]\n    memory[\"last_action\"] = action\n    memory[\"last_bearing\"] = bearing\n    return action\n\n\ndef termination_contract(observation, memory):\n    \"\"\"Return a high-confidence stationary fallback when no move is admissible.\"\"\"\n    memory[\"state\"] = \"STOPPED\"\n    return \"STOP\", 0.98\n\n\ndef decide(observation, memory):\n    \"\"\"Compose safety, tracking, approach, progress, and termination decisions.\"\"\"\n    memory = dict(memory or {})\n\n    safety = safety_contract(observation, memory)\n    if safety is not None:\n        return safety[0], safety[1], memory\n\n    tracking = tracking_contract(observation, memory)\n    if tracking is not None:\n        return tracking[0], tracking[1], memory\n\n    memory[\"state\"] = \"LOCKED\"\n    if observation[\"target_distance_class\"] == \"near\":\n        if (\n            abs(float(observation[\"heading_error\"])) <= 15.0\n            and float(observation[\"target_identity_confidence\"]) >= 0.80\n        ):\n            memory[\"state\"] = \"ARRIVED\"\n            return \"ARRIVED\", 0.96, memory\n\n        side = (\n            \"VEER_LEFT\"\n            if float(observation[\"target_bearing\"]) < 0.0\n            else \"VEER_RIGHT\"\n        )\n        if (\n            (side == \"VEER_LEFT\" and observation[\"corridor_left\"])\n            or (side == \"VEER_RIGHT\" and observation[\"corridor_right\"])\n        ):\n            candidates = [side]\n        elif observation[\"corridor_center\"]:\n            candidates = [\"FORWARD\"]\n        else:\n            candidates = []\n    else:\n        candidates = propose_moves(observation, memory)\n\n    action = progress_contract(observation, memory, candidates)\n    if action is None:\n        fallback = termination_contract(observation, memory)\n        return fallback[0], fallback[1], memory\n\n    memory[\"state\"] = \"APPROACH\"\n    return action, 0.90, memory\n\n\n# EVOLVE-BLOCK-END",
      "solution_sha256": "08574cf59a1b37a0d30debbf46c6d1cead62ae490b5c388ed93241e1d816a7ae"
    }
  ]
}
