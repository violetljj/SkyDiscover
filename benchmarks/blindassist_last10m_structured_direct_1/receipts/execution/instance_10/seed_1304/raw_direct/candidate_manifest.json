{
  "candidates": [
    {
      "id": "29f9ee2d-3901-45a0-a963-27df5593ce62",
      "iteration_found": 1,
      "solution": "# EVOLVE-BLOCK-START\ndef decide(observation, memory):\n    \"\"\"Use a safety-gated, hysteretic target tracker with conservative steering.\"\"\"\n    memory = dict(memory or {})\n    state = memory.get(\"state\", \"IDLE\")\n    scan_direction = memory.get(\"scan_direction\", \"SCAN_LEFT\")\n    previous_action = memory.get(\"previous_action\", \"\")\n\n    visible = bool(observation[\"target_visible\"])\n    bearing = float(observation[\"target_bearing\"])\n    distance = observation[\"target_distance_class\"]\n    target_confidence = float(observation[\"target_confidence\"])\n    identity_confidence = float(observation[\"target_identity_confidence\"])\n    track_age = int(observation[\"track_age\"])\n    lost_steps = int(observation[\"lost_steps\"])\n    safety_confidence = float(observation[\"safety_confidence\"])\n    closing_risk = float(observation[\"closing_risk\"])\n    heading_error = abs(float(observation[\"heading_error\"]))\n\n    if safety_confidence < 0.60 or closing_risk >= 0.75:\n        memory[\"state\"] = \"STOPPED\"\n        memory[\"previous_action\"] = \"STOP\"\n        return \"STOP\", 0.99, memory\n\n    confirmed = (\n        visible\n        and target_confidence >= 0.55\n        and identity_confidence >= 0.60\n    )\n\n    if not confirmed:\n        if state in {\"LOCKED\", \"APPROACH\", \"REACQUIRE\"} or lost_steps > 0:\n            memory[\"state\"] = \"REACQUIRE\"\n            if lost_steps <= 1:\n                memory[\"previous_action\"] = \"STOP\"\n                return \"STOP\", 0.92, memory\n\n        action = scan_direction\n        memory[\"scan_direction\"] = (\n            \"SCAN_RIGHT\" if scan_direction == \"SCAN_LEFT\" else \"SCAN_LEFT\"\n        )\n        memory[\"state\"] = \"SEARCH\"\n        memory[\"previous_action\"] = action\n        return action, 0.75, memory\n\n    if track_age < 2:\n        memory[\"state\"] = \"VERIFY\"\n        memory[\"previous_action\"] = \"SLOW_DOWN\"\n        return \"SLOW_DOWN\", 0.80, memory\n\n    memory[\"state\"] = \"LOCKED\"\n\n    if distance == \"near\":\n        if heading_error <= 15.0 and identity_confidence >= 0.80:\n            memory[\"state\"] = \"ARRIVED\"\n            memory[\"previous_action\"] = \"ARRIVED\"\n            return \"ARRIVED\", 0.96, memory\n\n        action = \"VEER_LEFT\" if bearing < 0.0 else \"VEER_RIGHT\"\n        memory[\"state\"] = \"APPROACH\"\n        memory[\"previous_action\"] = action\n        return action, 0.88, memory\n\n    memory[\"state\"] = \"APPROACH\"\n\n    # Keep a wider deadband to avoid unnecessary zig-zagging and path loss.\n    if abs(bearing) <= 16.0 and observation[\"corridor_center\"]:\n        memory[\"previous_action\"] = \"FORWARD\"\n        return \"FORWARD\", 0.92, memory\n\n    if bearing < -16.0 and observation[\"corridor_left\"]:\n        memory[\"previous_action\"] = \"VEER_LEFT\"\n        return \"VEER_LEFT\", 0.90, memory\n\n    if bearing > 16.0 and observation[\"corridor_right\"]:\n        memory[\"previous_action\"] = \"VEER_RIGHT\"\n        return \"VEER_RIGHT\", 0.90, memory\n\n    if observation[\"corridor_center\"]:\n        memory[\"previous_action\"] = \"FORWARD\"\n        return \"FORWARD\", 0.86, memory\n\n    # Prefer the side indicated by the target when the center corridor is closed.\n    if bearing <= 0.0 and observation[\"corridor_left\"]:\n        memory[\"previous_action\"] = \"VEER_LEFT\"\n        return \"VEER_LEFT\", 0.82, memory\n\n    if bearing > 0.0 and observation[\"corridor_right\"]:\n        memory[\"previous_action\"] = \"VEER_RIGHT\"\n        return \"VEER_RIGHT\", 0.82, memory\n\n    memory[\"state\"] = \"STOPPED\"\n    memory[\"previous_action\"] = \"STOP\"\n    return \"STOP\", 0.98, memory\n# EVOLVE-BLOCK-END",
      "solution_sha256": "dec7f8ac7cd541e5de18cb476343dbce6c399683029325cc2c733c034dc37e80"
    }
  ]
}
