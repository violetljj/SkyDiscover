{
  "candidates": [
    {
      "id": "20576f83-ccdc-4b73-b680-b7a8f08a6676",
      "iteration_found": 1,
      "solution": "\"\"\"Temporal, safety-first candidate generator with bounded progress memory.\"\"\"\n\n# EVOLVE-BLOCK-START\nMOTION_ACTIONS = (\"FORWARD\", \"VEER_LEFT\", \"VEER_RIGHT\")\n\n\ndef safety_contract(observation, memory):\n    \"\"\"Stop whenever confidence is weak or forward motion has closing risk.\"\"\"\n    if (\n        float(observation[\"safety_confidence\"]) < 0.60\n        or float(observation[\"closing_risk\"]) >= 0.75\n    ):\n        memory[\"state\"] = \"STOPPED\"\n        memory[\"last_action\"] = \"STOP\"\n        return \"STOP\", 0.99\n    return None\n\n\ndef tracking_contract(observation, memory):\n    \"\"\"Require stable identity and use hysteretic, non-advancing reacquisition.\"\"\"\n    visible = bool(observation[\"target_visible\"])\n    reliable = (\n        visible\n        and float(observation[\"target_confidence\"]) >= 0.55\n        and float(observation[\"target_identity_confidence\"]) >= 0.60\n    )\n\n    if not reliable:\n        lost_steps = max(0, int(observation[\"lost_steps\"]))\n        memory[\"lost_steps_seen\"] = lost_steps\n\n        if (\n            memory.get(\"state\") in {\"LOCKED\", \"APPROACH\", \"REACQUIRE\", \"VERIFY\"}\n            and lost_steps <= 1\n        ):\n            memory[\"state\"] = \"REACQUIRE\"\n            memory[\"last_action\"] = \"STOP\"\n            return \"STOP\", 0.92\n\n        direction = memory.get(\"scan_direction\", \"SCAN_LEFT\")\n        if lost_steps >= 2 and lost_steps % 2 == 0:\n            direction = \"SCAN_RIGHT\" if direction == \"SCAN_LEFT\" else \"SCAN_LEFT\"\n        memory[\"scan_direction\"] = direction\n        memory[\"state\"] = \"SEARCH\"\n        memory[\"last_action\"] = direction\n        return direction, 0.75\n\n    if int(observation[\"track_age\"]) < 2:\n        memory[\"state\"] = \"VERIFY\"\n        memory[\"last_action\"] = \"SLOW_DOWN\"\n        return \"SLOW_DOWN\", 0.80\n\n    memory[\"lost_steps_seen\"] = 0\n    return None\n\n\ndef propose_moves(observation, memory):\n    \"\"\"Build deduplicated moves, prioritizing the corridor matching target bearing.\"\"\"\n    bearing = float(observation[\"target_bearing\"])\n    candidates = []\n\n    if bearing < -12.0 and bool(observation[\"corridor_left\"]):\n        candidates.append(\"VEER_LEFT\")\n    if bearing > 12.0 and bool(observation[\"corridor_right\"]):\n        candidates.append(\"VEER_RIGHT\")\n    if bool(observation[\"corridor_center\"]):\n        candidates.append(\"FORWARD\")\n    if bool(observation[\"corridor_left\"]):\n        candidates.append(\"VEER_LEFT\")\n    if bool(observation[\"corridor_right\"]):\n        candidates.append(\"VEER_RIGHT\")\n\n    return list(dict.fromkeys(candidates))\n\n\ndef progress_contract(observation, memory, candidates):\n    \"\"\"Choose the best aligned move while damping repeated turns and short oscillations.\"\"\"\n    if not candidates:\n        return None\n\n    bearing = float(observation[\"target_bearing\"])\n    heading = float(observation[\"heading_error\"])\n    last = memory.get(\"last_action\")\n    stall = int(memory.get(\"stall_count\", 0))\n\n    def score(action):\n        value = 0.0\n        if action == \"FORWARD\":\n            value += 2.0 - min(abs(bearing), 45.0) / 20.0\n            value += 1.0 - min(abs(heading), 45.0) / 30.0\n        elif action == \"VEER_LEFT\":\n            value += 2.5 if bearing < 0.0 else -1.0\n            value += 1.0 if heading < 0.0 else -0.5\n        elif action == \"VEER_RIGHT\":\n            value += 2.5 if bearing > 0.0 else -1.0\n            value += 1.0 if heading > 0.0 else -0.5\n\n        if action == last:\n            value += 0.35\n            if stall >= 2 and action != \"FORWARD\":\n                value -= 1.4\n        if (\n            last == \"VEER_LEFT\" and action == \"VEER_RIGHT\"\n        ) or (\n            last == \"VEER_RIGHT\" and action == \"VEER_LEFT\"\n        ):\n            value -= 1.2\n        return value\n\n    action = max(candidates, key=score)\n    if action == last:\n        memory[\"stall_count\"] = stall + 1\n    else:\n        memory[\"stall_count\"] = 0\n    return action\n\n\ndef termination_contract(observation, memory):\n    \"\"\"Return a conservative stationary fallback when no movement is admissible.\"\"\"\n    memory[\"state\"] = \"STOPPED\"\n    memory[\"last_action\"] = \"STOP\"\n    return \"STOP\", 0.98\n\n\ndef decide(observation, memory):\n    \"\"\"Apply safety, tracking, arrival, proposal, progress, and termination contracts.\"\"\"\n    memory = dict(memory or {})\n\n    safety = safety_contract(observation, memory)\n    if safety is not None:\n        return safety[0], safety[1], memory\n\n    tracking = tracking_contract(observation, memory)\n    if tracking is not None:\n        return tracking[0], tracking[1], memory\n\n    memory[\"state\"] = \"LOCKED\"\n    if (\n        observation[\"target_distance_class\"] == \"near\"\n        and abs(float(observation[\"heading_error\"])) <= 15.0\n        and float(observation[\"target_identity_confidence\"]) >= 0.80\n    ):\n        memory[\"state\"] = \"ARRIVED\"\n        memory[\"last_action\"] = \"ARRIVED\"\n        return \"ARRIVED\", 0.96, memory\n\n    if observation[\"target_distance_class\"] == \"near\":\n        bearing = float(observation[\"target_bearing\"])\n        candidates = [\n            \"VEER_LEFT\" if bearing < 0.0 else \"VEER_RIGHT\"\n        ]\n    else:\n        candidates = propose_moves(observation, memory)\n\n    action = progress_contract(observation, memory, candidates)\n    if action is None:\n        fallback = termination_contract(observation, memory)\n        return fallback[0], fallback[1], memory\n\n    memory[\"state\"] = \"APPROACH\"\n    memory[\"last_action\"] = action\n    return action, 0.90, memory\n\n\n# EVOLVE-BLOCK-END",
      "solution_sha256": "e5dcb4fc84328b3de4244ba07fd3f4847662902975a362115d1b72b2815f94bf"
    }
  ]
}
