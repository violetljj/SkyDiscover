{
  "candidates": [
    {
      "id": "5a587f13-f6e7-4531-9ea0-db83c66cc6ec",
      "iteration_found": 1,
      "solution": "\"\"\"Temporal, safety-first candidate policy with bounded hysteresis and state memory.\"\"\"\n\n# EVOLVE-BLOCK-START\nMOTION_ACTIONS = (\"FORWARD\", \"VEER_LEFT\", \"VEER_RIGHT\")\n\n\ndef safety_contract(observation, memory):\n    \"\"\"Stop immediately when confidence is low or forward closing risk is high.\"\"\"\n    if (\n        float(observation.get(\"safety_confidence\", 0.0)) < 0.60\n        or float(observation.get(\"closing_risk\", 1.0)) >= 0.75\n    ):\n        memory[\"state\"] = \"STOPPED\"\n        memory[\"last_action\"] = \"STOP\"\n        return \"STOP\", 0.99\n    return None\n\n\ndef tracking_contract(observation, memory):\n    \"\"\"Hold or scan conservatively until target visibility and identity are reliable.\"\"\"\n    visible = bool(observation.get(\"target_visible\", False))\n    reliable = (\n        visible\n        and float(observation.get(\"target_confidence\", 0.0)) >= 0.55\n        and float(observation.get(\"target_identity_confidence\", 0.0)) >= 0.60\n    )\n    if reliable:\n        if int(observation.get(\"track_age\", 0)) < 2:\n            memory[\"state\"] = \"VERIFY\"\n            memory[\"last_action\"] = \"SLOW_DOWN\"\n            return \"SLOW_DOWN\", 0.80\n        return None\n\n    lost_steps = int(observation.get(\"lost_steps\", 0))\n    prior_state = memory.get(\"state\")\n    if prior_state in {\"LOCKED\", \"APPROACH\", \"REACQUIRE\", \"VERIFY\"} and lost_steps <= 1:\n        memory[\"state\"] = \"REACQUIRE\"\n        memory[\"last_action\"] = \"STOP\"\n        return \"STOP\", 0.92\n\n    direction = memory.get(\"scan_direction\", \"SCAN_LEFT\")\n    if lost_steps > 1:\n        direction = \"SCAN_RIGHT\" if direction == \"SCAN_LEFT\" else \"SCAN_LEFT\"\n        memory[\"scan_direction\"] = direction\n    memory[\"state\"] = \"SEARCH\"\n    memory[\"last_action\"] = direction\n    return direction, 0.75\n\n\ndef propose_moves(observation, memory):\n    \"\"\"Build a deduplicated movement set from visible corridor openings and bearing.\"\"\"\n    bearing = float(observation.get(\"target_bearing\", 0.0))\n    candidates = []\n\n    if bearing < -12.0 and bool(observation.get(\"corridor_left\", False)):\n        candidates.append(\"VEER_LEFT\")\n    elif bearing > 12.0 and bool(observation.get(\"corridor_right\", False)):\n        candidates.append(\"VEER_RIGHT\")\n\n    if bool(observation.get(\"corridor_center\", False)):\n        candidates.append(\"FORWARD\")\n    if bool(observation.get(\"corridor_left\", False)):\n        candidates.append(\"VEER_LEFT\")\n    if bool(observation.get(\"corridor_right\", False)):\n        candidates.append(\"VEER_RIGHT\")\n\n    return list(dict.fromkeys(candidates))\n\n\ndef progress_contract(observation, memory, candidates):\n    \"\"\"Choose the least-corrective action while applying hysteresis to prevent oscillation.\"\"\"\n    if not candidates:\n        return None\n\n    bearing = float(observation.get(\"target_bearing\", 0.0))\n    last = memory.get(\"last_action\")\n    previous_bearing = memory.get(\"bearing\")\n    if isinstance(previous_bearing, (int, float)):\n        delta = abs(bearing - previous_bearing)\n    else:\n        delta = 0.0\n\n    preferred = None\n    if bearing < -12.0:\n        preferred = \"VEER_LEFT\"\n    elif bearing > 12.0:\n        preferred = \"VEER_RIGHT\"\n    elif \"FORWARD\" in candidates:\n        preferred = \"FORWARD\"\n\n    # Keep a previous correction briefly when the bearing is stable.\n    if (\n        last in {\"VEER_LEFT\", \"VEER_RIGHT\"}\n        and last in candidates\n        and abs(bearing) <= 20.0\n        and delta < 8.0\n    ):\n        preferred = last\n\n    if preferred not in candidates:\n        preferred = candidates[0]\n\n    memory[\"bearing\"] = bearing\n    memory[\"last_action\"] = preferred\n    return preferred\n\n\ndef termination_contract(observation, memory):\n    \"\"\"Return a high-confidence stationary fallback when no safe movement is available.\"\"\"\n    memory[\"state\"] = \"STOPPED\"\n    memory[\"last_action\"] = \"STOP\"\n    return \"STOP\", 0.98\n\n\ndef decide(observation, memory):\n    \"\"\"Compose safety, tracking, arrival, progress, and termination into a three-value result.\"\"\"\n    memory = dict(memory or {})\n\n    safety = safety_contract(observation, memory)\n    if safety is not None:\n        return safety[0], safety[1], memory\n\n    tracking = tracking_contract(observation, memory)\n    if tracking is not None:\n        return tracking[0], tracking[1], memory\n\n    memory[\"state\"] = \"LOCKED\"\n    distance_class = observation.get(\"target_distance_class\")\n    identity_confidence = float(observation.get(\"target_identity_confidence\", 0.0))\n    heading_error = abs(float(observation.get(\"heading_error\", 180.0)))\n\n    if distance_class == \"near\":\n        if heading_error <= 15.0 and identity_confidence >= 0.80:\n            memory[\"state\"] = \"ARRIVED\"\n            memory[\"last_action\"] = \"ARRIVED\"\n            return \"ARRIVED\", 0.96, memory\n\n        bearing = float(observation.get(\"target_bearing\", 0.0))\n        candidates = [\n            \"VEER_LEFT\" if bearing < 0.0 else \"VEER_RIGHT\"\n        ]\n    else:\n        candidates = propose_moves(observation, memory)\n\n    action = progress_contract(observation, memory, candidates)\n    if action is None:\n        fallback = termination_contract(observation, memory)\n        return fallback[0], fallback[1], memory\n\n    memory[\"state\"] = \"APPROACH\"\n    return action, 0.90, memory\n\n\n# EVOLVE-BLOCK-END",
      "solution_sha256": "c3014afc8c6e39c13b7e7c72bec70f87bc5561d8b9fe9bc08a2b60ddce572a7e"
    }
  ]
}
