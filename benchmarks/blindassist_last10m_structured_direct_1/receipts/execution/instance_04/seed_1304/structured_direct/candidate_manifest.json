{
  "candidates": [
    {
      "id": "98516bfb-21e4-43f8-9a30-765e304e0fae",
      "iteration_found": 1,
      "solution": "\"\"\"Temporal, safety-first candidate policy with bounded reacquisition and hysteresis.\"\"\"\n\n# EVOLVE-BLOCK-START\nMOTION_ACTIONS = (\"FORWARD\", \"VEER_LEFT\", \"VEER_RIGHT\")\n\n\ndef safety_contract(observation, memory):\n    \"\"\"Stop whenever confidence is low or collision-closing risk is high.\"\"\"\n    if (\n        float(observation[\"safety_confidence\"]) < 0.60\n        or float(observation[\"closing_risk\"]) >= 0.75\n    ):\n        memory[\"state\"] = \"STOPPED\"\n        return \"STOP\", 0.99\n    return None\n\n\ndef tracking_contract(observation, memory):\n    \"\"\"Use short confirmation stops and persistent, bounded scan directions when tracking is weak.\"\"\"\n    reliable = (\n        bool(observation[\"target_visible\"])\n        and float(observation[\"target_confidence\"]) >= 0.55\n        and float(observation[\"target_identity_confidence\"]) >= 0.60\n    )\n    if reliable:\n        memory[\"scan_count\"] = 0\n        if int(observation[\"track_age\"]) < 2:\n            memory[\"state\"] = \"VERIFY\"\n            return \"SLOW_DOWN\", 0.80\n        return None\n\n    lost_steps = int(observation[\"lost_steps\"])\n    if memory.get(\"state\") in {\"LOCKED\", \"APPROACH\", \"REACQUIRE\"} and lost_steps <= 2:\n        memory[\"state\"] = \"REACQUIRE\"\n        return \"STOP\", 0.92\n\n    direction = memory.get(\"scan_direction\", \"SCAN_LEFT\")\n    scan_count = int(memory.get(\"scan_count\", 0)) + 1\n    if scan_count >= 3:\n        direction = \"SCAN_RIGHT\" if direction == \"SCAN_LEFT\" else \"SCAN_LEFT\"\n        scan_count = 0\n    memory[\"scan_direction\"] = direction\n    memory[\"scan_count\"] = scan_count\n    memory[\"state\"] = \"SEARCH\"\n    return direction, 0.75\n\n\ndef propose_moves(observation, memory):\n    \"\"\"Generate de-duplicated motion proposals from bearing and currently open corridors.\"\"\"\n    bearing = float(observation[\"target_bearing\"])\n    candidates = []\n    if bearing < -12.0 and observation[\"corridor_left\"]:\n        candidates.append(\"VEER_LEFT\")\n    if bearing > 12.0 and observation[\"corridor_right\"]:\n        candidates.append(\"VEER_RIGHT\")\n    if observation[\"corridor_center\"]:\n        candidates.append(\"FORWARD\")\n    if observation[\"corridor_left\"]:\n        candidates.append(\"VEER_LEFT\")\n    if observation[\"corridor_right\"]:\n        candidates.append(\"VEER_RIGHT\")\n    return list(dict.fromkeys(candidates))\n\n\ndef progress_contract(observation, memory, candidates):\n    \"\"\"Rank candidates by bearing alignment while adding hysteresis against repeated veer oscillation.\"\"\"\n    if not candidates:\n        return None\n\n    bearing = float(observation[\"target_bearing\"])\n    previous = memory.get(\"last_action\")\n    previous_bearing = memory.get(\"last_bearing\")\n    scores = {}\n\n    for action in candidates:\n        score = 0.0\n        if action == \"FORWARD\":\n            score = 2.0 - min(abs(bearing) / 30.0, 1.5)\n        elif action == \"VEER_LEFT\":\n            score = 1.5 if bearing < 0.0 else 0.2\n            score -= min(abs(bearing) / 90.0, 0.5) if bearing >= 0.0 else 0.0\n        elif action == \"VEER_RIGHT\":\n            score = 1.5 if bearing > 0.0 else 0.2\n            score -= min(abs(bearing) / 90.0, 0.5) if bearing <= 0.0 else 0.0\n\n        if action == previous and action != \"FORWARD\":\n            score -= 0.35\n        if (\n            previous in {\"VEER_LEFT\", \"VEER_RIGHT\"}\n            and action != previous\n            and previous_bearing is not None\n            and bearing * float(previous_bearing) < 0.0\n        ):\n            score += 0.30\n        scores[action] = score\n\n    action = max(candidates, key=lambda item: scores[item])\n    memory[\"last_action\"] = action\n    memory[\"last_bearing\"] = bearing\n    return action\n\n\ndef termination_contract(observation, memory):\n    \"\"\"Return a high-confidence stop whenever no safe movement proposal is available.\"\"\"\n    memory[\"state\"] = \"STOPPED\"\n    return \"STOP\", 0.98\n\n\ndef decide(observation, memory):\n    \"\"\"Compose safety, tracking, arrival, proposal, progress, and termination contracts.\"\"\"\n    memory = dict(memory or {})\n\n    safety = safety_contract(observation, memory)\n    if safety is not None:\n        return safety[0], safety[1], memory\n\n    tracking = tracking_contract(observation, memory)\n    if tracking is not None:\n        return tracking[0], tracking[1], memory\n\n    memory[\"state\"] = \"LOCKED\"\n    if observation[\"target_distance_class\"] == \"near\":\n        if (\n            abs(float(observation[\"heading_error\"])) <= 15.0\n            and float(observation[\"target_identity_confidence\"]) >= 0.80\n        ):\n            memory[\"state\"] = \"ARRIVED\"\n            return \"ARRIVED\", 0.96, memory\n\n        bearing = float(observation[\"target_bearing\"])\n        candidates = []\n        if bearing < 0.0 and observation[\"corridor_left\"]:\n            candidates.append(\"VEER_LEFT\")\n        elif bearing >= 0.0 and observation[\"corridor_right\"]:\n            candidates.append(\"VEER_RIGHT\")\n        if not candidates:\n            candidates = propose_moves(observation, memory)\n    else:\n        candidates = propose_moves(observation, memory)\n\n    action = progress_contract(observation, memory, candidates)\n    if action is None:\n        fallback = termination_contract(observation, memory)\n        return fallback[0], fallback[1], memory\n\n    memory[\"state\"] = \"APPROACH\"\n    return action, 0.90, memory\n\n\n# EVOLVE-BLOCK-END",
      "solution_sha256": "253955e5c96f1464f161f08ace550758af05d17d69c90ff53e3a2414d3f92ae1"
    }
  ]
}
