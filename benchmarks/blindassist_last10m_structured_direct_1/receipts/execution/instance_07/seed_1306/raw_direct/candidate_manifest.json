{
  "candidates": [
    {
      "id": "e9d8d80b-8fec-4134-97a3-f893a21e974e",
      "iteration_found": 1,
      "solution": "# EVOLVE-BLOCK-START\ndef decide(observation, memory):\n    \"\"\"Use safety gating, confidence confirmation, persistent reacquisition,\n    and bearing hysteresis to approach a visible target with fewer action flips.\n    \"\"\"\n    memory = dict(memory or {})\n    state = memory.get(\"state\", \"IDLE\")\n    scan_direction = memory.get(\"scan_direction\", \"SCAN_LEFT\")\n    scan_steps = int(memory.get(\"scan_steps\", 0))\n    previous_action = memory.get(\"previous_action\", \"\")\n\n    visible = bool(observation[\"target_visible\"])\n    bearing = float(observation[\"target_bearing\"])\n    distance = observation[\"target_distance_class\"]\n    target_confidence = float(observation[\"target_confidence\"])\n    identity_confidence = float(observation[\"target_identity_confidence\"])\n    track_age = int(observation[\"track_age\"])\n    lost_steps = int(observation[\"lost_steps\"])\n    safety_confidence = float(observation[\"safety_confidence\"])\n    closing_risk = float(observation[\"closing_risk\"])\n\n    def emit(action, confidence, next_state):\n        memory[\"state\"] = next_state\n        memory[\"previous_action\"] = action\n        return action, confidence, memory\n\n    if safety_confidence < 0.60 or closing_risk >= 0.75:\n        return emit(\"STOP\", 0.99, \"STOPPED\")\n\n    confirmed = (\n        visible\n        and target_confidence >= 0.55\n        and identity_confidence >= 0.60\n    )\n\n    if not confirmed:\n        memory[\"scan_steps\"] = scan_steps\n        if state in {\"LOCKED\", \"APPROACH\", \"REACQUIRE\"} or lost_steps > 0:\n            if lost_steps <= 1:\n                return emit(\"STOP\", 0.92, \"REACQUIRE\")\n            if scan_steps >= 2:\n                scan_direction = (\n                    \"SCAN_RIGHT\" if scan_direction == \"SCAN_LEFT\"\n                    else \"SCAN_LEFT\"\n                )\n                scan_steps = 0\n            memory[\"scan_direction\"] = scan_direction\n            memory[\"scan_steps\"] = scan_steps + 1\n            return emit(scan_direction, 0.75, \"REACQUIRE\")\n        return emit(scan_direction, 0.75, \"SEARCH\")\n\n    memory[\"scan_steps\"] = 0\n    memory[\"scan_direction\"] = scan_direction\n\n    if track_age < 2:\n        return emit(\"SLOW_DOWN\", 0.80, \"VERIFY\")\n\n    if distance == \"near\":\n        if abs(float(observation[\"heading_error\"])) <= 15.0 and identity_confidence >= 0.80:\n            return emit(\"ARRIVED\", 0.96, \"ARRIVED\")\n        action = \"VEER_LEFT\" if bearing < 0 else \"VEER_RIGHT\"\n        return emit(action, 0.88, \"APPROACH\")\n\n    action = None\n    if bearing < -12.0 and observation[\"corridor_left\"]:\n        action = \"VEER_LEFT\"\n    elif bearing > 12.0 and observation[\"corridor_right\"]:\n        action = \"VEER_RIGHT\"\n    elif previous_action == \"VEER_LEFT\" and bearing < 6.0 and observation[\"corridor_left\"]:\n        action = \"VEER_LEFT\"\n    elif previous_action == \"VEER_RIGHT\" and bearing > -6.0 and observation[\"corridor_right\"]:\n        action = \"VEER_RIGHT\"\n    elif observation[\"corridor_center\"]:\n        action = \"FORWARD\"\n    elif observation[\"corridor_left\"]:\n        action = \"VEER_LEFT\"\n    elif observation[\"corridor_right\"]:\n        action = \"VEER_RIGHT\"\n\n    if action is None:\n        return emit(\"STOP\", 0.98, \"STOPPED\")\n    return emit(action, 0.90 if action in {\"FORWARD\", \"VEER_LEFT\", \"VEER_RIGHT\"} else 0.82, \"APPROACH\")\n# EVOLVE-BLOCK-END",
      "solution_sha256": "c216d6aca75dd2f9d0a02824f874e177ce13cce3e50f76f6fb5fa1eff8e48934"
    }
  ]
}
