{
  "candidates": [
    {
      "id": "fd3bf04a-8bf7-4d01-9268-dac8ed6a1774",
      "iteration_found": 1,
      "solution": "\"\"\"Temporal, safety-first candidate controller with hysteresis and bounded memory.\"\"\"\n\n# EVOLVE-BLOCK-START\nMOTION_ACTIONS = (\"FORWARD\", \"VEER_LEFT\", \"VEER_RIGHT\")\n\n\ndef safety_contract(observation, memory):\n    \"\"\"Stop whenever confidence is low or forward motion is hazardous.\"\"\"\n    if (\n        float(observation[\"safety_confidence\"]) < 0.60\n        or float(observation[\"closing_risk\"]) >= 0.75\n    ):\n        memory[\"state\"] = \"STOPPED\"\n        memory.pop(\"last_action\", None)\n        return \"STOP\", 0.99\n    return None\n\n\ndef tracking_contract(observation, memory):\n    \"\"\"Require stable identity and use non-advancing recovery while reacquiring.\"\"\"\n    reliable = (\n        bool(observation[\"target_visible\"])\n        and float(observation[\"target_confidence\"]) >= 0.55\n        and float(observation[\"target_identity_confidence\"]) >= 0.60\n    )\n    if reliable:\n        if int(observation[\"track_age\"]) < 2:\n            memory[\"state\"] = \"VERIFY\"\n            return \"SLOW_DOWN\", 0.80\n        return None\n\n    lost_steps = int(observation[\"lost_steps\"])\n    if memory.get(\"state\") in {\"LOCKED\", \"APPROACH\", \"REACQUIRE\"} and lost_steps <= 1:\n        memory[\"state\"] = \"REACQUIRE\"\n        return \"STOP\", 0.92\n\n    direction = memory.get(\"scan_direction\", \"SCAN_LEFT\")\n    memory[\"scan_direction\"] = \"SCAN_RIGHT\" if direction == \"SCAN_LEFT\" else \"SCAN_LEFT\"\n    memory[\"state\"] = \"SEARCH\"\n    return direction, 0.75\n\n\ndef propose_moves(observation, memory):\n    \"\"\"Build de-duplicated moves ordered by current bearing and corridor availability.\"\"\"\n    bearing = float(observation[\"target_bearing\"])\n    candidates = []\n\n    if bearing < -12.0 and observation[\"corridor_left\"]:\n        candidates.append(\"VEER_LEFT\")\n    elif bearing > 12.0 and observation[\"corridor_right\"]:\n        candidates.append(\"VEER_RIGHT\")\n\n    if observation[\"corridor_center\"]:\n        candidates.append(\"FORWARD\")\n    if observation[\"corridor_left\"]:\n        candidates.append(\"VEER_LEFT\")\n    if observation[\"corridor_right\"]:\n        candidates.append(\"VEER_RIGHT\")\n\n    return list(dict.fromkeys(candidates))\n\n\ndef progress_contract(observation, memory, candidates):\n    \"\"\"Select a direction with short-term hysteresis to suppress oscillation.\"\"\"\n    if not candidates:\n        return None\n\n    previous = memory.get(\"last_action\")\n    streak = int(memory.get(\"action_streak\", 0))\n    bearing = float(observation[\"target_bearing\"])\n    heading = abs(float(observation[\"heading_error\"]))\n\n    # Keep a successful directional correction briefly instead of immediately\n    # reversing on noisy bearings; permit forward when alignment is restored.\n    if previous in candidates and streak < 2:\n        if previous == \"FORWARD\" and heading <= 20.0:\n            choice = \"FORWARD\"\n        elif previous != \"FORWARD\" and abs(bearing) > 8.0:\n            choice = previous\n        else:\n            choice = \"FORWARD\" if \"FORWARD\" in candidates else previous\n    else:\n        choice = candidates[0]\n        if heading <= 15.0 and \"FORWARD\" in candidates:\n            choice = \"FORWARD\"\n\n    if choice == previous:\n        memory[\"action_streak\"] = streak + 1\n    else:\n        memory[\"action_streak\"] = 1\n    memory[\"last_action\"] = choice\n    return choice\n\n\ndef termination_contract(observation, memory):\n    \"\"\"Return a high-confidence stationary fallback when no move is admissible.\"\"\"\n    memory[\"state\"] = \"STOPPED\"\n    memory.pop(\"last_action\", None)\n    memory[\"action_streak\"] = 0\n    return \"STOP\", 0.98\n\n\ndef decide(observation, memory):\n    \"\"\"Compose safety, tracking, arrival, progress, and termination contracts.\"\"\"\n    memory = dict(memory or {})\n\n    safety = safety_contract(observation, memory)\n    if safety is not None:\n        return safety[0], safety[1], memory\n\n    tracking = tracking_contract(observation, memory)\n    if tracking is not None:\n        return tracking[0], tracking[1], memory\n\n    memory[\"state\"] = \"LOCKED\"\n    if observation[\"target_distance_class\"] == \"near\":\n        if (\n            abs(float(observation[\"heading_error\"])) <= 15.0\n            and float(observation[\"target_identity_confidence\"]) >= 0.80\n        ):\n            memory[\"state\"] = \"ARRIVED\"\n            memory.pop(\"last_action\", None)\n            return \"ARRIVED\", 0.96, memory\n        candidates = [\n            \"VEER_LEFT\" if float(observation[\"target_bearing\"]) < 0.0 else \"VEER_RIGHT\"\n        ]\n    else:\n        candidates = propose_moves(observation, memory)\n\n    action = progress_contract(observation, memory, candidates)\n    if action is None:\n        fallback = termination_contract(observation, memory)\n        return fallback[0], fallback[1], memory\n\n    memory[\"state\"] = \"APPROACH\"\n    return action, 0.90, memory\n\n\n# EVOLVE-BLOCK-END",
      "solution_sha256": "6ec9999da42c38e4607160f51a7e08393f6186a9e5532b2ccd5d3d93910af726"
    }
  ]
}
