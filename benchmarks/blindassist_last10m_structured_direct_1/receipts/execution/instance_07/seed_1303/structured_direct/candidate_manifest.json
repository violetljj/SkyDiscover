{
  "candidates": [
    {
      "id": "41205fcb-ef35-430f-af98-2ea7d9969f5c",
      "iteration_found": 1,
      "solution": "\"\"\"Temporal, safety-gated candidate controller with bounded progress memory.\"\"\"\n\n# EVOLVE-BLOCK-START\nMOTION_ACTIONS = (\"FORWARD\", \"VEER_LEFT\", \"VEER_RIGHT\")\n\n\ndef safety_contract(observation, memory):\n    \"\"\"Stop whenever confidence is low or collision-closing risk is excessive.\"\"\"\n    if (\n        float(observation.get(\"safety_confidence\", 0.0)) < 0.60\n        or float(observation.get(\"closing_risk\", 1.0)) >= 0.75\n    ):\n        memory[\"state\"] = \"STOPPED\"\n        memory[\"last_action\"] = \"STOP\"\n        return \"STOP\", 0.99\n    return None\n\n\ndef tracking_contract(observation, memory):\n    \"\"\"Require a stable identity and use bounded reacquisition when tracking fails.\"\"\"\n    visible = bool(observation.get(\"target_visible\", False))\n    reliable = (\n        visible\n        and float(observation.get(\"target_confidence\", 0.0)) >= 0.55\n        and float(observation.get(\"target_identity_confidence\", 0.0)) >= 0.60\n    )\n    if reliable:\n        if int(observation.get(\"track_age\", 0)) < 2:\n            memory[\"state\"] = \"VERIFY\"\n            memory[\"last_action\"] = \"SLOW_DOWN\"\n            return \"SLOW_DOWN\", 0.80\n        memory[\"scan_direction\"] = memory.get(\"scan_direction\", \"SCAN_LEFT\")\n        return None\n\n    lost = int(observation.get(\"lost_steps\", 0))\n    previous = memory.get(\"state\")\n    if previous in {\"LOCKED\", \"APPROACH\", \"REACQUIRE\"} and lost <= 1:\n        memory[\"state\"] = \"REACQUIRE\"\n        memory[\"last_action\"] = \"STOP\"\n        return \"STOP\", 0.92\n\n    direction = memory.get(\"scan_direction\", \"SCAN_LEFT\")\n    if lost >= 2:\n        direction = \"SCAN_RIGHT\" if direction == \"SCAN_LEFT\" else \"SCAN_LEFT\"\n    memory[\"scan_direction\"] = direction\n    memory[\"state\"] = \"SEARCH\"\n    memory[\"last_action\"] = direction\n    return direction, 0.75\n\n\ndef propose_moves(observation, memory):\n    \"\"\"Build deduplicated motion proposals from bearing and currently open corridors.\"\"\"\n    bearing = float(observation.get(\"target_bearing\", 0.0))\n    candidates = []\n    if bearing < -12.0 and observation.get(\"corridor_left\", False):\n        candidates.append(\"VEER_LEFT\")\n    if bearing > 12.0 and observation.get(\"corridor_right\", False):\n        candidates.append(\"VEER_RIGHT\")\n    if observation.get(\"corridor_center\", False):\n        candidates.append(\"FORWARD\")\n    if observation.get(\"corridor_left\", False):\n        candidates.append(\"VEER_LEFT\")\n    if observation.get(\"corridor_right\", False):\n        candidates.append(\"VEER_RIGHT\")\n    return list(dict.fromkeys(candidates))\n\n\ndef progress_contract(observation, memory, candidates):\n    \"\"\"Choose the best aligned proposal while damping reversals and repeated stalls.\"\"\"\n    if not candidates:\n        return None\n\n    bearing = float(observation.get(\"target_bearing\", 0.0))\n    heading = abs(float(observation.get(\"heading_error\", 0.0)))\n    last = memory.get(\"last_action\")\n    stall = int(memory.get(\"stall_count\", 0))\n    scored = []\n\n    for action in candidates:\n        score = 0.0\n        if action == \"FORWARD\":\n            score += 1.0 if abs(bearing) <= 20.0 else 0.15\n            score += 0.25 if heading <= 20.0 else 0.0\n        elif action == \"VEER_LEFT\":\n            score += max(0.0, min(1.0, (-bearing) / 90.0))\n        elif action == \"VEER_RIGHT\":\n            score += max(0.0, min(1.0, bearing / 90.0))\n\n        if action == last:\n            score += 0.12\n        if (\n            (last == \"VEER_LEFT\" and action == \"VEER_RIGHT\")\n            or (last == \"VEER_RIGHT\" and action == \"VEER_LEFT\")\n        ):\n            score -= 0.35\n        if stall >= 3 and action == \"FORWARD\":\n            score -= 0.20\n        scored.append((score, action))\n\n    scored.sort(key=lambda item: item[0], reverse=True)\n    action = scored[0][1]\n    memory[\"stall_count\"] = min(4, stall + (1 if action == last else 0))\n    memory[\"last_action\"] = action\n    return action\n\n\ndef termination_contract(observation, memory):\n    \"\"\"Return a conservative stop action when no safe movement is admissible.\"\"\"\n    memory[\"state\"] = \"STOPPED\"\n    memory[\"last_action\"] = \"STOP\"\n    return \"STOP\", 0.98\n\n\ndef decide(observation, memory):\n    \"\"\"Compose safety, tracking, arrival, proposal, progress, and fallback contracts.\"\"\"\n    memory = dict(memory or {})\n\n    safety = safety_contract(observation, memory)\n    if safety is not None:\n        return safety[0], safety[1], memory\n\n    tracking = tracking_contract(observation, memory)\n    if tracking is not None:\n        return tracking[0], tracking[1], memory\n\n    memory[\"state\"] = \"LOCKED\"\n    near = observation.get(\"target_distance_class\") == \"near\"\n    identity = float(observation.get(\"target_identity_confidence\", 0.0))\n    heading_error = abs(float(observation.get(\"heading_error\", 0.0)))\n\n    if near and heading_error <= 15.0 and identity >= 0.80:\n        memory[\"state\"] = \"ARRIVED\"\n        memory[\"last_action\"] = \"ARRIVED\"\n        return \"ARRIVED\", 0.96, memory\n\n    if near:\n        bearing = float(observation.get(\"target_bearing\", 0.0))\n        preferred = \"VEER_LEFT\" if bearing < 0.0 else \"VEER_RIGHT\"\n        candidates = [preferred]\n        for action in propose_moves(observation, memory):\n            if action not in candidates:\n                candidates.append(action)\n    else:\n        candidates = propose_moves(observation, memory)\n\n    action = progress_contract(observation, memory, candidates)\n    if action is None:\n        fallback = termination_contract(observation, memory)\n        return fallback[0], fallback[1], memory\n\n    memory[\"state\"] = \"APPROACH\"\n    return action, 0.90, memory\n\n\n# EVOLVE-BLOCK-END",
      "solution_sha256": "ede2a89a9077f96335de0429d7af09eb0ba2024ea078709802025a71c00b77e5"
    }
  ]
}
