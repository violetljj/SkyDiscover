{
  "candidates": [
    {
      "id": "841f5ee0-023d-45c9-a5d5-565ca35b755b",
      "iteration_found": 1,
      "solution": "\"\"\"Compact temporal navigation policy preserving the evaluator contracts.\"\"\"\n\n# EVOLVE-BLOCK-START\nMOTION_ACTIONS = (\"FORWARD\", \"VEER_LEFT\", \"VEER_RIGHT\")\n\n\ndef safety_contract(observation, memory):\n    \"\"\"Stop whenever confidence is low or forward closure risk is excessive.\"\"\"\n    if (\n        float(observation.get(\"safety_confidence\", 0.0)) < 0.60\n        or float(observation.get(\"closing_risk\", 1.0)) >= 0.75\n    ):\n        memory[\"state\"] = \"STOPPED\"\n        return \"STOP\", 0.99\n    return None\n\n\ndef tracking_contract(observation, memory):\n    \"\"\"Use brief stabilization on fresh tracks and steady bounded reacquisition.\"\"\"\n    visible = bool(observation.get(\"target_visible\", False))\n    reliable = (\n        visible\n        and float(observation.get(\"target_confidence\", 0.0)) >= 0.55\n        and float(observation.get(\"target_identity_confidence\", 0.0)) >= 0.60\n    )\n    if reliable:\n        if int(observation.get(\"track_age\", 0)) < 2:\n            memory[\"state\"] = \"VERIFY\"\n            return \"SLOW_DOWN\", 0.80\n        memory[\"scan_direction\"] = memory.get(\"scan_direction\", \"SCAN_LEFT\")\n        return None\n\n    lost_steps = int(observation.get(\"lost_steps\", 0))\n    if memory.get(\"state\") in {\"LOCKED\", \"APPROACH\", \"REACQUIRE\"} and lost_steps <= 1:\n        memory[\"state\"] = \"REACQUIRE\"\n        return \"STOP\", 0.92\n\n    direction = memory.get(\"scan_direction\", \"SCAN_LEFT\")\n    if lost_steps >= 2:\n        direction = \"SCAN_RIGHT\" if direction == \"SCAN_LEFT\" else \"SCAN_LEFT\"\n        memory[\"scan_direction\"] = direction\n    memory[\"state\"] = \"SEARCH\"\n    return direction, 0.75\n\n\ndef propose_moves(observation, memory):\n    \"\"\"Generate directionally useful moves while preferring straight travel near alignment.\"\"\"\n    bearing = float(observation.get(\"target_bearing\", 0.0))\n    left = bool(observation.get(\"corridor_left\", False))\n    right = bool(observation.get(\"corridor_right\", False))\n    center = bool(observation.get(\"corridor_center\", False))\n    candidates = []\n\n    if abs(bearing) <= 12.0 and center:\n        candidates.append(\"FORWARD\")\n    elif bearing < -12.0 and left:\n        candidates.append(\"VEER_LEFT\")\n    elif bearing > 12.0 and right:\n        candidates.append(\"VEER_RIGHT\")\n\n    if center:\n        candidates.append(\"FORWARD\")\n    if left:\n        candidates.append(\"VEER_LEFT\")\n    if right:\n        candidates.append(\"VEER_RIGHT\")\n    return list(dict.fromkeys(candidates))\n\n\ndef progress_contract(observation, memory, candidates):\n    \"\"\"Select a valid move using action persistence, bounded hysteresis, and turn relief.\"\"\"\n    if not candidates:\n        return None\n\n    previous = memory.get(\"last_action\")\n    repeats = int(memory.get(\"action_repeats\", 0))\n    bearing = float(observation.get(\"target_bearing\", 0.0))\n\n    if previous in candidates and repeats < 3:\n        if previous == \"FORWARD\" and abs(bearing) <= 18.0:\n            choice = previous\n        elif previous != \"FORWARD\" and abs(bearing) > 8.0:\n            choice = previous\n        else:\n            choice = candidates[0]\n    else:\n        choice = candidates[0]\n\n    if repeats >= 3 and len(candidates) > 1:\n        alternatives = [item for item in candidates if item != previous]\n        if alternatives:\n            choice = alternatives[0]\n\n    if choice == previous:\n        memory[\"action_repeats\"] = repeats + 1\n    else:\n        memory[\"action_repeats\"] = 1\n    memory[\"last_action\"] = choice\n    return choice\n\n\ndef termination_contract(observation, memory):\n    \"\"\"Return a high-confidence stationary fallback when no motion is admissible.\"\"\"\n    memory[\"state\"] = \"STOPPED\"\n    return \"STOP\", 0.98\n\n\ndef decide(observation, memory):\n    \"\"\"Compose safety, tracking, progress, and termination into a three-value result.\"\"\"\n    memory = dict(memory or {})\n\n    safety = safety_contract(observation, memory)\n    if safety is not None:\n        return safety[0], safety[1], memory\n\n    tracking = tracking_contract(observation, memory)\n    if tracking is not None:\n        return tracking[0], tracking[1], memory\n\n    memory[\"state\"] = \"LOCKED\"\n    near = observation.get(\"target_distance_class\") == \"near\"\n    if near:\n        if (\n            abs(float(observation.get(\"heading_error\", 999.0))) <= 15.0\n            and float(observation.get(\"target_identity_confidence\", 0.0)) >= 0.80\n        ):\n            memory[\"state\"] = \"ARRIVED\"\n            return \"ARRIVED\", 0.96, memory\n        bearing = float(observation.get(\"target_bearing\", 0.0))\n        candidates = [\n            \"VEER_LEFT\" if bearing < 0.0 else \"VEER_RIGHT\"\n        ]\n    else:\n        candidates = propose_moves(observation, memory)\n\n    action = progress_contract(observation, memory, candidates)\n    if action is None:\n        fallback = termination_contract(observation, memory)\n        return fallback[0], fallback[1], memory\n\n    memory[\"state\"] = \"APPROACH\"\n    return action, 0.90, memory\n\n\n# EVOLVE-BLOCK-END",
      "solution_sha256": "46371825cc4e409423b92c80642ba4955bb78486da8c21dfd3bbc19743de05b3"
    }
  ]
}
