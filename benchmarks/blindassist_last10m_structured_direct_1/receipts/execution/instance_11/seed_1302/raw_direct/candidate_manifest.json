{
  "candidates": [
    {
      "id": "8c0f0925-17c7-4dd6-8ecf-2496cdd25ed7",
      "iteration_found": 1,
      "solution": "def decide(observation, memory):\n    \"\"\"Use safety gating, confidence confirmation, steering hysteresis, and target-aligned motion.\"\"\"\n    memory = dict(memory or {})\n    state = memory.get(\"state\", \"IDLE\")\n    scan_direction = memory.get(\"scan_direction\", \"SCAN_LEFT\")\n    previous_action = memory.get(\"last_action\", \"\")\n\n    visible = bool(observation[\"target_visible\"])\n    bearing = float(observation[\"target_bearing\"])\n    distance = observation[\"target_distance_class\"]\n    target_confidence = float(observation[\"target_confidence\"])\n    identity_confidence = float(observation[\"target_identity_confidence\"])\n    track_age = int(observation[\"track_age\"])\n    lost_steps = int(observation[\"lost_steps\"])\n    safety_confidence = float(observation[\"safety_confidence\"])\n    closing_risk = float(observation[\"closing_risk\"])\n    heading_error = float(observation[\"heading_error\"])\n\n    def emit(action, confidence, next_state):\n        memory[\"state\"] = next_state\n        memory[\"last_action\"] = action\n        return action, confidence, memory\n\n    if safety_confidence < 0.60 or closing_risk >= 0.75:\n        return emit(\"STOP\", 0.99, \"STOPPED\")\n\n    confirmed = (\n        visible\n        and target_confidence >= 0.55\n        and identity_confidence >= 0.60\n    )\n    if not confirmed:\n        if state in {\"LOCKED\", \"APPROACH\", \"REACQUIRE\"} or lost_steps > 0:\n            if lost_steps <= 1:\n                return emit(\"STOP\", 0.92, \"REACQUIRE\")\n            next_direction = (\n                \"SCAN_RIGHT\" if scan_direction == \"SCAN_LEFT\" else \"SCAN_LEFT\"\n            )\n            memory[\"scan_direction\"] = next_direction\n            return emit(scan_direction, 0.75, \"SEARCH\")\n        return emit(scan_direction, 0.75, \"SEARCH\")\n\n    if track_age < 1:\n        return emit(\"SLOW_DOWN\", 0.80, \"VERIFY\")\n\n    if distance == \"near\":\n        if abs(heading_error) <= 15.0 and identity_confidence >= 0.80:\n            return emit(\"ARRIVED\", 0.96, \"ARRIVED\")\n        steer = bearing\n        if abs(steer) < 6.0:\n            steer = heading_error\n        if steer < -6.0 and observation[\"corridor_left\"]:\n            return emit(\"VEER_LEFT\", 0.88, \"APPROACH\")\n        if steer > 6.0 and observation[\"corridor_right\"]:\n            return emit(\"VEER_RIGHT\", 0.88, \"APPROACH\")\n        if observation[\"corridor_center\"]:\n            return emit(\"FORWARD\", 0.86, \"APPROACH\")\n\n    steer_threshold = 18.0\n    if bearing < -steer_threshold and observation[\"corridor_left\"]:\n        return emit(\"VEER_LEFT\", 0.90, \"APPROACH\")\n    if bearing > steer_threshold and observation[\"corridor_right\"]:\n        return emit(\"VEER_RIGHT\", 0.90, \"APPROACH\")\n\n    if observation[\"corridor_center\"]:\n        return emit(\"FORWARD\", 0.90, \"APPROACH\")\n\n    if bearing < 0.0 and observation[\"corridor_left\"]:\n        action = \"VEER_LEFT\"\n    elif bearing > 0.0 and observation[\"corridor_right\"]:\n        action = \"VEER_RIGHT\"\n    elif observation[\"corridor_left\"]:\n        action = \"VEER_LEFT\"\n    elif observation[\"corridor_right\"]:\n        action = \"VEER_RIGHT\"\n    else:\n        return emit(\"STOP\", 0.98, \"STOPPED\")\n\n    if (\n        previous_action in {\"VEER_LEFT\", \"VEER_RIGHT\"}\n        and action != previous_action\n        and abs(bearing) < 10.0\n        and observation[\"corridor_center\"]\n    ):\n        return emit(\"FORWARD\", 0.88, \"APPROACH\")\n    return emit(action, 0.82, \"APPROACH\")",
      "solution_sha256": "f7c47982fde208cef4a0bafc18b2c8bc4da7e2a6c72b8ee86f8e39966002d7e3"
    }
  ]
}
