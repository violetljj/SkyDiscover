{
  "candidates": [
    {
      "id": "f3bf10ac-91ed-464b-a3db-a2d1e79169f2",
      "iteration_found": 1,
      "solution": "\"\"\"Safe, memory-bounded temporal candidate policy for SkyDiscover.\"\"\"\n\n# EVOLVE-BLOCK-START\nMOTION_ACTIONS = (\"FORWARD\", \"VEER_LEFT\", \"VEER_RIGHT\")\n\n\ndef safety_contract(observation, memory):\n    \"\"\"Stop whenever confidence is low or forward motion has high collision risk.\"\"\"\n    if (\n        float(observation[\"safety_confidence\"]) < 0.60\n        or float(observation[\"closing_risk\"]) >= 0.75\n    ):\n        memory[\"state\"] = \"STOPPED\"\n        return \"STOP\", 0.99\n    return None\n\n\ndef tracking_contract(observation, memory):\n    \"\"\"Verify target identity, then use bounded non-motion recovery when tracking is weak.\"\"\"\n    reliable = (\n        bool(observation[\"target_visible\"])\n        and float(observation[\"target_confidence\"]) >= 0.55\n        and float(observation[\"target_identity_confidence\"]) >= 0.60\n    )\n    if reliable:\n        if int(observation[\"track_age\"]) < 2:\n            memory[\"state\"] = \"VERIFY\"\n            return \"SLOW_DOWN\", 0.80\n        return None\n\n    lost = max(0, int(observation[\"lost_steps\"]))\n    prior = memory.get(\"state\")\n    if prior in {\"LOCKED\", \"APPROACH\", \"REACQUIRE\"} and lost <= 1:\n        memory[\"state\"] = \"REACQUIRE\"\n        return \"STOP\", 0.92\n\n    direction = memory.get(\"scan_direction\", \"SCAN_LEFT\")\n    if lost >= 2 and lost % 2 == 0:\n        direction = \"SCAN_RIGHT\" if direction == \"SCAN_LEFT\" else \"SCAN_LEFT\"\n        memory[\"scan_direction\"] = direction\n    memory[\"state\"] = \"SEARCH\"\n    return direction, 0.75\n\n\ndef propose_moves(observation, memory):\n    \"\"\"Build an ordered, duplicate-free set of corridor moves from the current bearing.\"\"\"\n    bearing = float(observation[\"target_bearing\"])\n    moves = []\n    if abs(bearing) <= 12.0 and observation[\"corridor_center\"]:\n        moves.append(\"FORWARD\")\n    if bearing < -12.0 and observation[\"corridor_left\"]:\n        moves.append(\"VEER_LEFT\")\n    if bearing > 12.0 and observation[\"corridor_right\"]:\n        moves.append(\"VEER_RIGHT\")\n    if observation[\"corridor_center\"]:\n        moves.append(\"FORWARD\")\n    if observation[\"corridor_left\"]:\n        moves.append(\"VEER_LEFT\")\n    if observation[\"corridor_right\"]:\n        moves.append(\"VEER_RIGHT\")\n    return list(dict.fromkeys(move for move in moves if move in MOTION_ACTIONS))\n\n\ndef progress_contract(observation, memory, candidates):\n    \"\"\"Prefer bearing-correct motion while damping repeated actions after stalled progress.\"\"\"\n    if not candidates:\n        return None\n\n    bearing = float(observation[\"target_bearing\"])\n    heading = abs(float(observation[\"heading_error\"]))\n    previous = memory.get(\"last_action\")\n    stalled = int(memory.get(\"stalled_steps\", 0))\n\n    def score(action):\n        value = 0.0\n        if action == \"FORWARD\":\n            value += 2.0 if abs(bearing) <= 12.0 else -0.5\n            value += 0.5 if heading <= 15.0 else 0.0\n        elif action == \"VEER_LEFT\":\n            value += 2.0 if bearing < 0.0 else -0.5\n        elif action == \"VEER_RIGHT\":\n            value += 2.0 if bearing > 0.0 else -0.5\n        if action == previous:\n            value -= 0.8 if stalled >= 2 else 0.1\n        return value\n\n    action = max(candidates, key=score)\n    if action == previous:\n        memory[\"stalled_steps\"] = min(stalled + 1, 3)\n    else:\n        memory[\"stalled_steps\"] = 0\n    memory[\"last_action\"] = action\n    return action\n\n\ndef termination_contract(observation, memory):\n    \"\"\"Return a high-confidence stationary fallback when no safe move is available.\"\"\"\n    memory[\"state\"] = \"STOPPED\"\n    return \"STOP\", 0.98\n\n\ndef decide(observation, memory):\n    \"\"\"Compose safety, tracking, progress, and termination into (action, confidence, memory).\"\"\"\n    memory = dict(memory or {})\n\n    safety = safety_contract(observation, memory)\n    if safety is not None:\n        return safety[0], safety[1], memory\n\n    tracking = tracking_contract(observation, memory)\n    if tracking is not None:\n        return tracking[0], tracking[1], memory\n\n    memory[\"state\"] = \"LOCKED\"\n    near = observation[\"target_distance_class\"] == \"near\"\n    identity = float(observation[\"target_identity_confidence\"])\n    if near and abs(float(observation[\"heading_error\"])) <= 15.0 and identity >= 0.80:\n        memory[\"state\"] = \"ARRIVED\"\n        return \"ARRIVED\", 0.96, memory\n\n    if near:\n        bearing = float(observation[\"target_bearing\"])\n        candidates = [\"VEER_LEFT\" if bearing < 0.0 else \"VEER_RIGHT\"]\n    else:\n        candidates = propose_moves(observation, memory)\n\n    action = progress_contract(observation, memory, candidates)\n    if action is None:\n        fallback = termination_contract(observation, memory)\n        return fallback[0], fallback[1], memory\n\n    memory[\"state\"] = \"APPROACH\"\n    return action, 0.90, memory\n\n\n# EVOLVE-BLOCK-END",
      "solution_sha256": "7e67ab0a2ca5607520eca0f55b7ef10948981074b26142197bf7e4826242716d"
    }
  ]
}
