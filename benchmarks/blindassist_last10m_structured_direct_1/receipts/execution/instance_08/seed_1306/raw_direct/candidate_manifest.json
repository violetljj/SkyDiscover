{
  "candidates": [
    {
      "id": "cec28ec2-5858-4a5e-aea8-79da8cb7bac1",
      "iteration_found": 1,
      "solution": "\"\"\"L10M-ORACLE-3 conservative hysteresis policy.\"\"\"\n\n# EVOLVE-BLOCK-START\ndef decide(observation, memory):\n    \"\"\"Use safety gating, confidence confirmation, and hysteresis-based steering/search.\"\"\"\n    memory = dict(memory or {})\n    state = memory.get(\"state\", \"IDLE\")\n    scan_direction = memory.get(\"scan_direction\", \"SCAN_LEFT\")\n    search_age = int(memory.get(\"search_age\", 0))\n    last_action = memory.get(\"last_action\", \"\")\n\n    visible = bool(observation[\"target_visible\"])\n    bearing = float(observation[\"target_bearing\"])\n    distance = observation[\"target_distance_class\"]\n    target_confidence = float(observation[\"target_confidence\"])\n    identity_confidence = float(observation[\"target_identity_confidence\"])\n    track_age = int(observation[\"track_age\"])\n    lost_steps = int(observation[\"lost_steps\"])\n    safety_confidence = float(observation[\"safety_confidence\"])\n    closing_risk = float(observation[\"closing_risk\"])\n    heading_error = abs(float(observation[\"heading_error\"]))\n\n    def emit(action, confidence, next_state):\n        memory[\"state\"] = next_state\n        memory[\"last_action\"] = action\n        return action, confidence, memory\n\n    # Hard safety gate.\n    if safety_confidence < 0.60 or closing_risk >= 0.75:\n        return emit(\"STOP\", 0.99, \"STOPPED\")\n\n    reliable = (\n        visible\n        and target_confidence >= 0.55\n        and identity_confidence >= 0.60\n    )\n\n    if not reliable:\n        memory[\"search_age\"] = search_age\n        if lost_steps <= 1:\n            return emit(\"STOP\", 0.93, \"REACQUIRE\")\n\n        # Keep each scan direction for several frames to avoid left/right chatter.\n        search_age += 1\n        memory[\"search_age\"] = search_age\n        if search_age >= 3:\n            scan_direction = (\n                \"SCAN_RIGHT\" if scan_direction == \"SCAN_LEFT\" else \"SCAN_LEFT\"\n            )\n            memory[\"scan_direction\"] = scan_direction\n            memory[\"search_age\"] = 0\n        return emit(scan_direction, 0.74, \"SEARCH\")\n\n    memory[\"search_age\"] = 0\n\n    # Confirm a newly acquired track once, but do not repeatedly slow a stable one.\n    if track_age == 0:\n        return emit(\"SLOW_DOWN\", 0.82, \"VERIFY\")\n\n    if distance == \"near\":\n        if heading_error <= 15.0 and identity_confidence >= 0.80:\n            return emit(\"ARRIVED\", 0.96, \"ARRIVED\")\n\n        if bearing < 0 and observation[\"corridor_left\"]:\n            return emit(\"VEER_LEFT\", 0.88, \"APPROACH\")\n        if bearing >= 0 and observation[\"corridor_right\"]:\n            return emit(\"VEER_RIGHT\", 0.88, \"APPROACH\")\n        if observation[\"corridor_center\"]:\n            return emit(\"FORWARD\", 0.80, \"APPROACH\")\n        return emit(\"STOP\", 0.98, \"STOPPED\")\n\n    # Use a deadband and retain a valid previous turn near the boundary.\n    if bearing < -14.0:\n        if observation[\"corridor_left\"]:\n            return emit(\"VEER_LEFT\", 0.90, \"APPROACH\")\n        if observation[\"corridor_center\"]:\n            return emit(\"FORWARD\", 0.84, \"APPROACH\")\n    elif bearing > 14.0:\n        if observation[\"corridor_right\"]:\n            return emit(\"VEER_RIGHT\", 0.90, \"APPROACH\")\n        if observation[\"corridor_center\"]:\n            return emit(\"FORWARD\", 0.84, \"APPROACH\")\n    else:\n        if (\n            last_action == \"VEER_LEFT\"\n            and observation[\"corridor_left\"]\n            and bearing < 4.0\n        ):\n            return emit(\"VEER_LEFT\", 0.84, \"APPROACH\")\n        if (\n            last_action == \"VEER_RIGHT\"\n            and observation[\"corridor_right\"]\n            and bearing > -4.0\n        ):\n            return emit(\"VEER_RIGHT\", 0.84, \"APPROACH\")\n        if observation[\"corridor_center\"]:\n            return emit(\"FORWARD\", 0.91, \"APPROACH\")\n\n    if observation[\"corridor_left\"]:\n        return emit(\"VEER_LEFT\", 0.80, \"APPROACH\")\n    if observation[\"corridor_right\"]:\n        return emit(\"VEER_RIGHT\", 0.80, \"APPROACH\")\n    return emit(\"STOP\", 0.98, \"STOPPED\")\n# EVOLVE-BLOCK-END",
      "solution_sha256": "2233592d0a59c356a27caf1da092ae889ba1af6e260bc18a093bfce3b7f9fab1"
    }
  ]
}
