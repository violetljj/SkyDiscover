{
  "candidates": [
    {
      "id": "b6548c81-1e48-48bd-905e-0a16bb1fd9e9",
      "iteration_found": 1,
      "solution": "def decide(observation, memory):\n    \"\"\"Use safety gating, confidence hysteresis, stable reacquisition sweeps,\n    and heading-aware target alignment before advancing.\"\"\"\n    memory = dict(memory or {})\n    state = memory.get(\"state\", \"IDLE\")\n    scan_direction = memory.get(\"scan_direction\", \"SCAN_LEFT\")\n    scan_ticks = int(memory.get(\"scan_ticks\", 0))\n\n    visible = bool(observation[\"target_visible\"])\n    bearing = float(observation[\"target_bearing\"])\n    distance = observation[\"target_distance_class\"]\n    target_confidence = float(observation[\"target_confidence\"])\n    identity_confidence = float(observation[\"target_identity_confidence\"])\n    track_age = int(observation[\"track_age\"])\n    lost_steps = int(observation[\"lost_steps\"])\n    safety_confidence = float(observation[\"safety_confidence\"])\n    closing_risk = float(observation[\"closing_risk\"])\n    heading_error = float(observation[\"heading_error\"])\n\n    if safety_confidence < 0.60 or closing_risk >= 0.75:\n        memory.update(state=\"STOPPED\", scan_ticks=0)\n        return \"STOP\", 0.99, memory\n\n    confirmed = (\n        visible\n        and target_confidence >= 0.55\n        and identity_confidence >= 0.60\n    )\n    if not confirmed:\n        if state in {\"LOCKED\", \"APPROACH\", \"REACQUIRE\", \"VERIFY\"} or lost_steps > 0:\n            memory[\"state\"] = \"REACQUIRE\"\n            if lost_steps <= 1:\n                memory[\"scan_ticks\"] = 0\n                return \"STOP\", 0.92, memory\n\n        scan_ticks += 1\n        if scan_ticks >= 3:\n            scan_direction = (\n                \"SCAN_RIGHT\" if scan_direction == \"SCAN_LEFT\" else \"SCAN_LEFT\"\n            )\n            scan_ticks = 0\n        memory.update(\n            state=\"SEARCH\",\n            scan_direction=scan_direction,\n            scan_ticks=scan_ticks,\n        )\n        return scan_direction, 0.75, memory\n\n    memory[\"scan_ticks\"] = 0\n    if track_age < 2:\n        memory[\"state\"] = \"VERIFY\"\n        return \"SLOW_DOWN\", 0.80, memory\n\n    if distance == \"near\":\n        if abs(heading_error) <= 15.0 and identity_confidence >= 0.80:\n            memory[\"state\"] = \"ARRIVED\"\n            return \"ARRIVED\", 0.96, memory\n        memory[\"state\"] = \"APPROACH\"\n        if abs(bearing) > 8.0:\n            return (\"VEER_LEFT\" if bearing < 0 else \"VEER_RIGHT\"), 0.88, memory\n        return (\"VEER_LEFT\" if heading_error < 0 else \"VEER_RIGHT\"), 0.84, memory\n\n    memory[\"state\"] = \"APPROACH\"\n    if bearing < -10.0 and observation[\"corridor_left\"]:\n        return \"VEER_LEFT\", 0.90, memory\n    if bearing > 10.0 and observation[\"corridor_right\"]:\n        return \"VEER_RIGHT\", 0.90, memory\n    if observation[\"corridor_center\"] and abs(heading_error) <= 30.0:\n        return \"FORWARD\", 0.90, memory\n    if observation[\"corridor_left\"] and not observation[\"corridor_right\"]:\n        return \"VEER_LEFT\", 0.82, memory\n    if observation[\"corridor_right\"]:\n        return \"VEER_RIGHT\", 0.82, memory\n    memory[\"state\"] = \"STOPPED\"\n    return \"STOP\", 0.98, memory",
      "solution_sha256": "5a8765df0743912da200f0fd0d3c5e277beee0939851659ced212afd58b8ff1f"
    }
  ]
}
