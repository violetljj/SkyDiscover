{
  "candidates": [
    {
      "id": "fac3a0a8-0dc8-4744-bc63-606dbb334f7f",
      "iteration_found": 1,
      "solution": "\"\"\"Temporal, safety-gated candidate controller with bounded action hysteresis.\"\"\"\n\n# EVOLVE-BLOCK-START\nMOTION_ACTIONS = (\"FORWARD\", \"VEER_LEFT\", \"VEER_RIGHT\")\n\n\ndef safety_contract(observation, memory):\n    \"\"\"Stop immediately when confidence is low or collision risk is high.\"\"\"\n    if (\n        float(observation[\"safety_confidence\"]) < 0.60\n        or float(observation[\"closing_risk\"]) >= 0.75\n    ):\n        memory[\"state\"] = \"STOPPED\"\n        memory[\"last_action\"] = \"STOP\"\n        memory[\"action_streak\"] = 0\n        return \"STOP\", 0.99\n    return None\n\n\ndef tracking_contract(observation, memory):\n    \"\"\"Pause briefly for transient loss, then alternate bounded scan directions.\"\"\"\n    visible = bool(observation[\"target_visible\"])\n    reliable = (\n        visible\n        and float(observation[\"target_confidence\"]) >= 0.55\n        and float(observation[\"target_identity_confidence\"]) >= 0.60\n    )\n    if reliable:\n        if int(observation[\"track_age\"]) < 2:\n            memory[\"state\"] = \"VERIFY\"\n            return \"SLOW_DOWN\", 0.80\n        return None\n\n    lost_steps = int(observation[\"lost_steps\"])\n    prior_state = memory.get(\"state\")\n    if prior_state in {\"LOCKED\", \"APPROACH\", \"REACQUIRE\"} and lost_steps <= 1:\n        memory[\"state\"] = \"REACQUIRE\"\n        memory[\"last_action\"] = \"STOP\"\n        memory[\"action_streak\"] = 0\n        return \"STOP\", 0.92\n\n    direction = memory.get(\"scan_direction\", \"SCAN_LEFT\")\n    if lost_steps >= 2:\n        direction = \"SCAN_RIGHT\" if direction == \"SCAN_LEFT\" else \"SCAN_LEFT\"\n    memory[\"scan_direction\"] = direction\n    memory[\"state\"] = \"SEARCH\"\n    memory[\"last_action\"] = direction\n    memory[\"action_streak\"] = 0\n    return direction, 0.75\n\n\ndef propose_moves(observation, memory):\n    \"\"\"Generate deduplicated moves ordered by bearing alignment and corridor support.\"\"\"\n    bearing = float(observation[\"target_bearing\"])\n    candidates = []\n\n    if bearing <= -8.0 and observation[\"corridor_left\"]:\n        candidates.append(\"VEER_LEFT\")\n    elif bearing >= 8.0 and observation[\"corridor_right\"]:\n        candidates.append(\"VEER_RIGHT\")\n\n    if observation[\"corridor_center\"]:\n        candidates.append(\"FORWARD\")\n    if observation[\"corridor_left\"]:\n        candidates.append(\"VEER_LEFT\")\n    if observation[\"corridor_right\"]:\n        candidates.append(\"VEER_RIGHT\")\n\n    return list(dict.fromkeys(candidates))\n\n\ndef progress_contract(observation, memory, candidates):\n    \"\"\"Use bearing hysteresis and a short streak cap to reduce oscillation and switches.\"\"\"\n    if not candidates:\n        return None\n\n    bearing = float(observation[\"target_bearing\"])\n    previous = memory.get(\"last_action\")\n    streak = int(memory.get(\"action_streak\", 0))\n\n    if previous in candidates and streak < 3:\n        if previous == \"FORWARD\" or abs(bearing) <= 20.0:\n            return previous\n\n    if bearing < -8.0 and \"VEER_LEFT\" in candidates:\n        return \"VEER_LEFT\"\n    if bearing > 8.0 and \"VEER_RIGHT\" in candidates:\n        return \"VEER_RIGHT\"\n    if \"FORWARD\" in candidates:\n        return \"FORWARD\"\n    return candidates[0]\n\n\ndef termination_contract(observation, memory):\n    \"\"\"Return a high-confidence stop when no safe movement proposal is available.\"\"\"\n    memory[\"state\"] = \"STOPPED\"\n    memory[\"last_action\"] = \"STOP\"\n    memory[\"action_streak\"] = 0\n    return \"STOP\", 0.98\n\n\ndef decide(observation, memory):\n    \"\"\"Compose safety, tracking, arrival, progress, and termination contracts.\"\"\"\n    memory = dict(memory or {})\n\n    safety = safety_contract(observation, memory)\n    if safety is not None:\n        return safety[0], safety[1], memory\n\n    tracking = tracking_contract(observation, memory)\n    if tracking is not None:\n        return tracking[0], tracking[1], memory\n\n    memory[\"state\"] = \"LOCKED\"\n    if observation[\"target_distance_class\"] == \"near\":\n        if (\n            abs(float(observation[\"heading_error\"])) <= 15.0\n            and float(observation[\"target_identity_confidence\"]) >= 0.80\n        ):\n            memory[\"state\"] = \"ARRIVED\"\n            memory[\"last_action\"] = \"ARRIVED\"\n            memory[\"action_streak\"] = 0\n            return \"ARRIVED\", 0.96, memory\n        candidates = [\n            \"VEER_LEFT\"\n            if float(observation[\"target_bearing\"]) < 0.0\n            else \"VEER_RIGHT\"\n        ]\n    else:\n        candidates = propose_moves(observation, memory)\n\n    action = progress_contract(observation, memory, candidates)\n    if action is None:\n        fallback = termination_contract(observation, memory)\n        return fallback[0], fallback[1], memory\n\n    if action == memory.get(\"last_action\"):\n        memory[\"action_streak\"] = min(int(memory.get(\"action_streak\", 0)) + 1, 3)\n    else:\n        memory[\"action_streak\"] = 1\n    memory[\"last_action\"] = action\n    memory[\"state\"] = \"APPROACH\"\n    return action, 0.90, memory\n\n\n# EVOLVE-BLOCK-END",
      "solution_sha256": "7e8e73534768f5c083d27f048e6774f3a9b855a98b8b8ae7a87b3b36bf4d7b47"
    }
  ]
}
