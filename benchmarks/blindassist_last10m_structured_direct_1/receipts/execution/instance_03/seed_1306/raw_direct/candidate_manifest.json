{
  "candidates": [
    {
      "id": "e195e0ac-bed5-4dcf-87df-250fe0c40446",
      "iteration_found": 1,
      "solution": "# EVOLVE-BLOCK-START\ndef decide(observation, memory):\n    \"\"\"Use safety gating, confidence hysteresis, and proportional target tracking.\"\"\"\n    memory = dict(memory or {})\n    state = memory.get(\"state\", \"IDLE\")\n    scan_direction = memory.get(\"scan_direction\", \"SCAN_LEFT\")\n    last_action = memory.get(\"last_action\", \"\")\n    lost_count = int(memory.get(\"lost_count\", 0))\n\n    visible = bool(observation[\"target_visible\"])\n    bearing = float(observation[\"target_bearing\"])\n    distance = observation[\"target_distance_class\"]\n    target_confidence = float(observation[\"target_confidence\"])\n    identity_confidence = float(observation[\"target_identity_confidence\"])\n    track_age = int(observation[\"track_age\"])\n    lost_steps = int(observation[\"lost_steps\"])\n    safety_confidence = float(observation[\"safety_confidence\"])\n    closing_risk = float(observation[\"closing_risk\"])\n    heading_error = abs(float(observation[\"heading_error\"]))\n\n    def emit(action, confidence, next_state):\n        memory[\"state\"] = next_state\n        memory[\"last_action\"] = action\n        return action, confidence, memory\n\n    if safety_confidence < 0.60 or closing_risk >= 0.75:\n        return emit(\"STOP\", 0.99, \"STOPPED\")\n\n    reliable = (\n        visible\n        and target_confidence >= 0.55\n        and identity_confidence >= 0.60\n    )\n    if not reliable:\n        lost_count = max(lost_count + 1, lost_steps)\n        memory[\"lost_count\"] = lost_count\n        if state in {\"LOCKED\", \"APPROACH\", \"REACQUIRE\", \"VERIFY\"} or lost_count:\n            if lost_count <= 1:\n                return emit(\"STOP\", 0.92, \"REACQUIRE\")\n            action = scan_direction\n            memory[\"scan_direction\"] = (\n                \"SCAN_RIGHT\" if scan_direction == \"SCAN_LEFT\" else \"SCAN_LEFT\"\n            )\n            return emit(action, 0.75, \"SEARCH\")\n        return emit(scan_direction, 0.72, \"SEARCH\")\n\n    memory[\"lost_count\"] = 0\n    if track_age < 2:\n        return emit(\"SLOW_DOWN\", 0.80, \"VERIFY\")\n\n    if (\n        distance == \"near\"\n        and heading_error <= 15.0\n        and identity_confidence >= 0.80\n        and target_confidence >= 0.65\n    ):\n        return emit(\"ARRIVED\", 0.96, \"ARRIVED\")\n\n    left = bool(observation[\"corridor_left\"])\n    right = bool(observation[\"corridor_right\"])\n    center = bool(observation[\"corridor_center\"])\n\n    if bearing < -12.0 and left:\n        return emit(\"VEER_LEFT\", 0.90, \"APPROACH\")\n    if bearing > 12.0 and right:\n        return emit(\"VEER_RIGHT\", 0.90, \"APPROACH\")\n\n    if abs(bearing) <= 8.0 and center:\n        return emit(\"FORWARD\", 0.92, \"APPROACH\")\n\n    if bearing < 0.0 and left:\n        return emit(\"VEER_LEFT\", 0.84, \"APPROACH\")\n    if bearing > 0.0 and right:\n        return emit(\"VEER_RIGHT\", 0.84, \"APPROACH\")\n\n    if center:\n        return emit(\"FORWARD\", 0.86, \"APPROACH\")\n    if left:\n        return emit(\"VEER_LEFT\", 0.80, \"APPROACH\")\n    if right:\n        return emit(\"VEER_RIGHT\", 0.80, \"APPROACH\")\n\n    if last_action in {\"VEER_LEFT\", \"VEER_RIGHT\"}:\n        return emit(last_action, 0.70, \"APPROACH\")\n    return emit(\"STOP\", 0.98, \"STOPPED\")\n# EVOLVE-BLOCK-END",
      "solution_sha256": "a76952ae976cd84a51fdad1ae73ca4a0e78cd87bf1dfb151eb5ed1920e7aaaca"
    }
  ]
}
