{
  "candidates": [
    {
      "id": "dd74965a-75b6-435a-95e8-e289033a3fdf",
      "iteration_found": 1,
      "solution": "\"\"\"L10M conservative target-tracking policy with safety gating and hysteresis.\"\"\"\n\n\n# EVOLVE-BLOCK-START\ndef decide(observation, memory):\n    \"\"\"Choose a safe, stable action using confirmed target state and bounded memory.\"\"\"\n    memory = dict(memory or {})\n    state = memory.get(\"state\", \"IDLE\")\n    scan_direction = memory.get(\"scan_direction\", \"SCAN_LEFT\")\n    scan_hold = int(memory.get(\"scan_hold\", 0))\n\n    visible = bool(observation[\"target_visible\"])\n    bearing = float(observation[\"target_bearing\"])\n    distance = observation[\"target_distance_class\"]\n    target_confidence = float(observation[\"target_confidence\"])\n    identity_confidence = float(observation[\"target_identity_confidence\"])\n    track_age = int(observation[\"track_age\"])\n    lost_steps = int(observation[\"lost_steps\"])\n    safety_confidence = float(observation[\"safety_confidence\"])\n    closing_risk = float(observation[\"closing_risk\"])\n    heading_error = float(observation[\"heading_error\"])\n\n    # Safety is an absolute gate.\n    if safety_confidence < 0.60 or closing_risk >= 0.75:\n        memory[\"state\"] = \"STOPPED\"\n        memory[\"scan_hold\"] = 0\n        return \"STOP\", 0.99, memory\n\n    confirmed = (\n        visible\n        and target_confidence >= 0.55\n        and identity_confidence >= 0.60\n    )\n\n    if not confirmed:\n        memory[\"state\"] = \"REACQUIRE\" if (\n            state in {\"LOCKED\", \"APPROACH\", \"VERIFY\", \"REACQUIRE\"} or lost_steps > 0\n        ) else \"SEARCH\"\n\n        if lost_steps <= 1:\n            memory[\"scan_hold\"] = 0\n            return \"STOP\", 0.92, memory\n\n        if scan_hold <= 0:\n            scan_direction = (\n                \"SCAN_RIGHT\" if scan_direction == \"SCAN_LEFT\" else \"SCAN_LEFT\"\n            )\n            scan_hold = 2\n\n        memory[\"scan_direction\"] = scan_direction\n        memory[\"scan_hold\"] = scan_hold - 1\n        return scan_direction, 0.75, memory\n\n    memory[\"scan_hold\"] = 0\n\n    if track_age < 2:\n        memory[\"state\"] = \"VERIFY\"\n        return \"SLOW_DOWN\", 0.80, memory\n\n    if distance == \"near\":\n        if abs(heading_error) <= 15.0 and identity_confidence >= 0.80:\n            memory[\"state\"] = \"ARRIVED\"\n            return \"ARRIVED\", 0.96, memory\n\n        steering_error = bearing if abs(bearing) > 8.0 else heading_error\n        memory[\"state\"] = \"APPROACH\"\n        if abs(steering_error) <= 8.0:\n            return \"SLOW_DOWN\", 0.84, memory\n        return (\"VEER_LEFT\" if steering_error < 0 else \"VEER_RIGHT\"), 0.88, memory\n\n    memory[\"state\"] = \"APPROACH\"\n\n    # Use the central corridor whenever the target is only mildly off-axis;\n    # this reduces needless lateral oscillation and improves path efficiency.\n    if observation[\"corridor_center\"] and abs(bearing) <= 20.0:\n        return \"FORWARD\", 0.90, memory\n\n    if bearing < -20.0 and observation[\"corridor_left\"]:\n        return \"VEER_LEFT\", 0.90, memory\n    if bearing > 20.0 and observation[\"corridor_right\"]:\n        return \"VEER_RIGHT\", 0.90, memory\n\n    if observation[\"corridor_center\"]:\n        return \"FORWARD\", 0.86, memory\n    if bearing < 0.0 and observation[\"corridor_left\"]:\n        return \"VEER_LEFT\", 0.82, memory\n    if bearing > 0.0 and observation[\"corridor_right\"]:\n        return \"VEER_RIGHT\", 0.82, memory\n    if observation[\"corridor_left\"]:\n        return \"VEER_LEFT\", 0.78, memory\n    if observation[\"corridor_right\"]:\n        return \"VEER_RIGHT\", 0.78, memory\n\n    memory[\"state\"] = \"STOPPED\"\n    return \"STOP\", 0.98, memory\n\n\n# EVOLVE-BLOCK-END",
      "solution_sha256": "3bfcfdb2a95b7fa7b48d48736b766efc29fc846ffd44ecc06d17d5073bb5c15e"
    }
  ]
}
