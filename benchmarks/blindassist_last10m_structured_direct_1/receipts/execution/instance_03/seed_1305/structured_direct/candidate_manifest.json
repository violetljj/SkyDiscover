{
  "candidates": [
    {
      "id": "bf38325a-d99e-4f3c-a9fc-e5f002397949",
      "iteration_found": 1,
      "solution": "\"\"\"Temporal, safety-first candidate policy with bounded action hysteresis.\"\"\"\n\n# EVOLVE-BLOCK-START\nMOTION_ACTIONS = (\"FORWARD\", \"VEER_LEFT\", \"VEER_RIGHT\")\n\n\ndef safety_contract(observation, memory):\n    \"\"\"Stop whenever confidence is low or forward closure presents material risk.\"\"\"\n    if (\n        float(observation[\"safety_confidence\"]) < 0.60\n        or float(observation[\"closing_risk\"]) >= 0.75\n    ):\n        memory[\"state\"] = \"STOPPED\"\n        return \"STOP\", 0.99\n    return None\n\n\ndef tracking_contract(observation, memory):\n    \"\"\"Require stable identity and use bounded, stateful reacquisition when tracking degrades.\"\"\"\n    visible = bool(observation[\"target_visible\"])\n    reliable = (\n        visible\n        and float(observation[\"target_confidence\"]) >= 0.55\n        and float(observation[\"target_identity_confidence\"]) >= 0.60\n    )\n    if reliable:\n        if int(observation[\"track_age\"]) < 2:\n            memory[\"state\"] = \"VERIFY\"\n            return \"SLOW_DOWN\", 0.80\n        return None\n\n    lost_steps = max(0, int(observation[\"lost_steps\"]))\n    prior_state = memory.get(\"state\")\n    if prior_state in {\"LOCKED\", \"APPROACH\", \"REACQUIRE\"} and lost_steps <= 1:\n        memory[\"state\"] = \"REACQUIRE\"\n        return \"STOP\", 0.92\n\n    direction = memory.get(\"scan_direction\")\n    if direction not in {\"SCAN_LEFT\", \"SCAN_RIGHT\"}:\n        direction = \"SCAN_LEFT\"\n    if lost_steps > 0 and lost_steps % 2 == 0:\n        direction = \"SCAN_RIGHT\" if direction == \"SCAN_LEFT\" else \"SCAN_LEFT\"\n    memory[\"scan_direction\"] = direction\n    memory[\"state\"] = \"SEARCH\"\n    return direction, 0.75\n\n\ndef propose_moves(observation, memory):\n    \"\"\"Generate unique corridor-admissible moves, prioritizing the observed bearing.\"\"\"\n    bearing = float(observation[\"target_bearing\"])\n    candidates = []\n\n    if bearing < -10.0 and observation[\"corridor_left\"]:\n        candidates.append(\"VEER_LEFT\")\n    if bearing > 10.0 and observation[\"corridor_right\"]:\n        candidates.append(\"VEER_RIGHT\")\n    if observation[\"corridor_center\"]:\n        candidates.append(\"FORWARD\")\n    if observation[\"corridor_left\"]:\n        candidates.append(\"VEER_LEFT\")\n    if observation[\"corridor_right\"]:\n        candidates.append(\"VEER_RIGHT\")\n\n    return list(dict.fromkeys(candidates))\n\n\ndef progress_contract(observation, memory, candidates):\n    \"\"\"Score proposals by bearing alignment, corridor support, and short-term hysteresis.\"\"\"\n    if not candidates:\n        return None\n\n    bearing = float(observation[\"target_bearing\"])\n    previous = memory.get(\"last_action\")\n    scores = {}\n\n    for action in candidates:\n        if action == \"FORWARD\":\n            score = 2.0 - min(abs(bearing), 45.0) / 45.0\n            if observation[\"corridor_center\"]:\n                score += 0.8\n        elif action == \"VEER_LEFT\":\n            score = 1.4 if bearing < 0.0 else 0.25\n            if observation[\"corridor_left\"]:\n                score += 0.25\n        else:\n            score = 1.4 if bearing > 0.0 else 0.25\n            if observation[\"corridor_right\"]:\n                score += 0.25\n\n        if action == previous:\n            score += 0.20\n        elif (\n            previous == \"VEER_LEFT\"\n            and action == \"VEER_RIGHT\"\n            or previous == \"VEER_RIGHT\"\n            and action == \"VEER_LEFT\"\n        ):\n            if abs(bearing) < 10.0:\n                score -= 0.45\n\n        scores[action] = score\n\n    action = max(candidates, key=lambda item: scores[item])\n    memory[\"last_action\"] = action\n    memory[\"action_repeats\"] = (\n        int(memory.get(\"action_repeats\", 0)) + 1 if action == previous else 1\n    )\n    return action\n\n\ndef termination_contract(observation, memory):\n    \"\"\"Return a high-confidence stationary fallback when no safe proposal is available.\"\"\"\n    memory[\"state\"] = \"STOPPED\"\n    return \"STOP\", 0.98\n\n\ndef decide(observation, memory):\n    \"\"\"Compose safety, tracking, arrival, progress, and termination into a three-value result.\"\"\"\n    memory = dict(memory or {})\n\n    safety = safety_contract(observation, memory)\n    if safety is not None:\n        return safety[0], safety[1], memory\n\n    tracking = tracking_contract(observation, memory)\n    if tracking is not None:\n        return tracking[0], tracking[1], memory\n\n    memory[\"state\"] = \"LOCKED\"\n    if observation[\"target_distance_class\"] == \"near\":\n        if (\n            abs(float(observation[\"heading_error\"])) <= 15.0\n            and float(observation[\"target_identity_confidence\"]) >= 0.80\n        ):\n            memory[\"state\"] = \"ARRIVED\"\n            return \"ARRIVED\", 0.96, memory\n\n        bearing = float(observation[\"target_bearing\"])\n        candidates = [\n            \"VEER_LEFT\" if bearing < 0.0 else \"VEER_RIGHT\"\n        ]\n    else:\n        candidates = propose_moves(observation, memory)\n\n    action = progress_contract(observation, memory, candidates)\n    if action is None:\n        fallback = termination_contract(observation, memory)\n        return fallback[0], fallback[1], memory\n\n    memory[\"state\"] = \"APPROACH\"\n    return action, 0.90, memory\n\n\n# EVOLVE-BLOCK-END",
      "solution_sha256": "d9fa729145481bfee21e3ce6bed9c90d36dd7fb7c7ceddd68ea442568c4081c2"
    }
  ]
}
