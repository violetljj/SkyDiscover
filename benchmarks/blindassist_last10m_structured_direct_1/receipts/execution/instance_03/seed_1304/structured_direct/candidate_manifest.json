{
  "candidates": [
    {
      "id": "ed0784cd-237a-4380-9247-81fbd8b2ea7a",
      "iteration_found": 1,
      "solution": "\"\"\"Temporal, safety-first candidate controller with bounded hysteresis.\"\"\"\n\n# EVOLVE-BLOCK-START\nMOTION_ACTIONS = (\"FORWARD\", \"VEER_LEFT\", \"VEER_RIGHT\")\n\n\ndef safety_contract(observation, memory):\n    \"\"\"Stop whenever confidence is low or collision risk crosses the hard gate.\"\"\"\n    if (\n        float(observation[\"safety_confidence\"]) < 0.60\n        or float(observation[\"closing_risk\"]) >= 0.75\n    ):\n        memory[\"state\"] = \"STOPPED\"\n        return \"STOP\", 0.99\n    return None\n\n\ndef tracking_contract(observation, memory):\n    \"\"\"Require a reliable target and use bounded stopping or alternating scans after loss.\"\"\"\n    reliable = (\n        bool(observation[\"target_visible\"])\n        and float(observation[\"target_confidence\"]) >= 0.55\n        and float(observation[\"target_identity_confidence\"]) >= 0.60\n    )\n    if reliable:\n        if int(observation[\"track_age\"]) < 2:\n            memory[\"state\"] = \"VERIFY\"\n            return \"SLOW_DOWN\", 0.80\n        return None\n\n    lost = int(observation[\"lost_steps\"])\n    if memory.get(\"state\") in {\"LOCKED\", \"APPROACH\", \"REACQUIRE\"} and lost <= 1:\n        memory[\"state\"] = \"REACQUIRE\"\n        return \"STOP\", 0.92\n\n    direction = memory.get(\"scan_direction\", \"SCAN_LEFT\")\n    memory[\"scan_direction\"] = \"SCAN_RIGHT\" if direction == \"SCAN_LEFT\" else \"SCAN_LEFT\"\n    memory[\"state\"] = \"SEARCH\"\n    return direction, 0.75\n\n\ndef propose_moves(observation, memory):\n    \"\"\"Order feasible moves by current bearing while retaining straight and side escapes.\"\"\"\n    bearing = float(observation[\"target_bearing\"])\n    moves = []\n    if bearing < -10.0 and observation[\"corridor_left\"]:\n        moves.append(\"VEER_LEFT\")\n    if bearing > 10.0 and observation[\"corridor_right\"]:\n        moves.append(\"VEER_RIGHT\")\n    if observation[\"corridor_center\"]:\n        moves.append(\"FORWARD\")\n    if observation[\"corridor_left\"]:\n        moves.append(\"VEER_LEFT\")\n    if observation[\"corridor_right\"]:\n        moves.append(\"VEER_RIGHT\")\n    return list(dict.fromkeys(moves))\n\n\ndef progress_contract(observation, memory, candidates):\n    \"\"\"Apply heading hysteresis and bounded anti-oscillation using recent action memory.\"\"\"\n    if not candidates:\n        return None\n\n    bearing = float(observation[\"target_bearing\"])\n    previous = memory.get(\"last_action\")\n    repeats = int(memory.get(\"repeat_count\", 0))\n    last_bearing = memory.get(\"last_bearing\")\n    if previous in candidates and previous != \"FORWARD\":\n        improved = last_bearing is None or abs(bearing) < abs(float(last_bearing)) - 1.0\n        if repeats < 2 or improved:\n            choice = previous\n        else:\n            choice = next((x for x in candidates if x != previous), candidates[0])\n    elif abs(bearing) <= 8.0 and \"FORWARD\" in candidates:\n        choice = \"FORWARD\"\n    else:\n        choice = candidates[0]\n\n    memory[\"repeat_count\"] = repeats + 1 if choice == previous else 1\n    memory[\"last_action\"] = choice\n    memory[\"last_bearing\"] = bearing\n    return choice\n\n\ndef termination_contract(observation, memory):\n    \"\"\"Return a high-confidence stationary fallback when no motion is admissible.\"\"\"\n    memory[\"state\"] = \"STOPPED\"\n    return \"STOP\", 0.98\n\n\ndef decide(observation, memory):\n    \"\"\"Compose safety, tracking, arrival, proposal, progress, and termination contracts.\"\"\"\n    memory = dict(memory or {})\n\n    safety = safety_contract(observation, memory)\n    if safety is not None:\n        return safety[0], safety[1], memory\n\n    tracking = tracking_contract(observation, memory)\n    if tracking is not None:\n        return tracking[0], tracking[1], memory\n\n    memory[\"state\"] = \"LOCKED\"\n    near = observation[\"target_distance_class\"] == \"near\"\n    if near and (\n        abs(float(observation[\"heading_error\"])) <= 15.0\n        and float(observation[\"target_identity_confidence\"]) >= 0.80\n    ):\n        memory[\"state\"] = \"ARRIVED\"\n        return \"ARRIVED\", 0.96, memory\n\n    if near:\n        bearing = float(observation[\"target_bearing\"])\n        candidates = [\n            \"VEER_LEFT\" if bearing < 0.0 else \"VEER_RIGHT\"\n        ]\n    else:\n        candidates = propose_moves(observation, memory)\n\n    action = progress_contract(observation, memory, candidates)\n    if action is None:\n        fallback = termination_contract(observation, memory)\n        return fallback[0], fallback[1], memory\n\n    memory[\"state\"] = \"APPROACH\"\n    return action, 0.90, memory\n\n\n# EVOLVE-BLOCK-END",
      "solution_sha256": "9c9ef04ab377cb37712a188bc544b2520e3625253eece032e5982c02f0721af5"
    }
  ]
}
