{
  "candidates": [
    {
      "id": "fbddbe13-3338-4e76-b083-f71435828f9f",
      "iteration_found": 1,
      "solution": "\"\"\"Temporal candidate controller with safety gating, stable reacquisition, and\nbearing-aware progress selection.\"\"\"\n\n# EVOLVE-BLOCK-START\nMOTION_ACTIONS = (\"FORWARD\", \"VEER_LEFT\", \"VEER_RIGHT\")\n\n\ndef safety_contract(observation, memory):\n    \"\"\"Stop whenever confidence is low or forward motion presents closing risk.\"\"\"\n    if (\n        float(observation[\"safety_confidence\"]) < 0.60\n        or float(observation[\"closing_risk\"]) >= 0.75\n    ):\n        memory[\"state\"] = \"STOPPED\"\n        return \"STOP\", 0.99\n    return None\n\n\ndef tracking_contract(observation, memory):\n    \"\"\"Hold a bounded reacquisition action before alternating scan directions.\"\"\"\n    visible = bool(observation[\"target_visible\"])\n    reliable = (\n        visible\n        and float(observation[\"target_confidence\"]) >= 0.55\n        and float(observation[\"target_identity_confidence\"]) >= 0.60\n    )\n    if reliable:\n        memory[\"lost_steps\"] = 0\n        if int(observation[\"track_age\"]) < 2:\n            memory[\"state\"] = \"VERIFY\"\n            return \"SLOW_DOWN\", 0.80\n        return None\n\n    lost_steps = int(observation[\"lost_steps\"])\n    previous_state = memory.get(\"state\")\n    if previous_state in {\"LOCKED\", \"APPROACH\", \"REACQUIRE\"} and lost_steps <= 1:\n        memory[\"state\"] = \"REACQUIRE\"\n        memory[\"lost_steps\"] = lost_steps\n        return \"STOP\", 0.92\n\n    direction = memory.get(\"scan_direction\", \"SCAN_LEFT\")\n    if lost_steps >= 2:\n        direction = \"SCAN_RIGHT\" if direction == \"SCAN_LEFT\" else \"SCAN_LEFT\"\n    memory[\"scan_direction\"] = direction\n    memory[\"lost_steps\"] = lost_steps\n    memory[\"state\"] = \"SEARCH\"\n    return direction, 0.75\n\n\ndef propose_moves(observation, memory):\n    \"\"\"Build deduplicated moves from corridor availability and target bearing.\"\"\"\n    bearing = float(observation[\"target_bearing\"])\n    candidates = []\n    if bearing < -10.0 and observation[\"corridor_left\"]:\n        candidates.append(\"VEER_LEFT\")\n    if bearing > 10.0 and observation[\"corridor_right\"]:\n        candidates.append(\"VEER_RIGHT\")\n    if observation[\"corridor_center\"]:\n        candidates.append(\"FORWARD\")\n    if observation[\"corridor_left\"]:\n        candidates.append(\"VEER_LEFT\")\n    if observation[\"corridor_right\"]:\n        candidates.append(\"VEER_RIGHT\")\n    return list(dict.fromkeys(candidates))\n\n\ndef progress_contract(observation, memory, candidates):\n    \"\"\"Choose the move with best expected bearing correction and discourage oscillation.\"\"\"\n    if not candidates:\n        return None\n\n    bearing = float(observation[\"target_bearing\"])\n    previous = memory.get(\"last_action\")\n    scores = {}\n    for action in candidates:\n        if action == \"FORWARD\":\n            score = 2.0 - min(abs(bearing), 90.0) / 45.0\n        elif action == \"VEER_LEFT\":\n            score = 2.2 if bearing < 0.0 else 0.8\n        else:\n            score = 2.2 if bearing > 0.0 else 0.8\n\n        if (\n            (previous == \"VEER_LEFT\" and action == \"VEER_RIGHT\")\n            or (previous == \"VEER_RIGHT\" and action == \"VEER_LEFT\")\n        ):\n            score -= 0.55\n        if action == previous:\n            score += 0.12\n        scores[action] = score\n\n    action = max(candidates, key=lambda item: scores[item])\n    if action == previous:\n        memory[\"streak\"] = int(memory.get(\"streak\", 0)) + 1\n    else:\n        memory[\"streak\"] = 1\n    memory[\"last_action\"] = action\n    memory[\"last_bearing\"] = bearing\n    return action\n\n\ndef termination_contract(observation, memory):\n    \"\"\"Return a high-confidence stationary fallback when no move is admissible.\"\"\"\n    memory[\"state\"] = \"STOPPED\"\n    return \"STOP\", 0.98\n\n\ndef decide(observation, memory):\n    \"\"\"Compose safety, tracking, arrival, progress, and termination into three outputs.\"\"\"\n    memory = dict(memory or {})\n\n    safety = safety_contract(observation, memory)\n    if safety is not None:\n        return safety[0], safety[1], memory\n\n    tracking = tracking_contract(observation, memory)\n    if tracking is not None:\n        return tracking[0], tracking[1], memory\n\n    memory[\"state\"] = \"LOCKED\"\n    if observation[\"target_distance_class\"] == \"near\":\n        if (\n            abs(float(observation[\"heading_error\"])) <= 15.0\n            and float(observation[\"target_identity_confidence\"]) >= 0.80\n        ):\n            memory[\"state\"] = \"ARRIVED\"\n            return \"ARRIVED\", 0.96, memory\n        bearing = float(observation[\"target_bearing\"])\n        candidates = [\n            \"VEER_LEFT\" if bearing < 0.0 else \"VEER_RIGHT\"\n        ]\n    else:\n        candidates = propose_moves(observation, memory)\n\n    action = progress_contract(observation, memory, candidates)\n    if action is None:\n        fallback = termination_contract(observation, memory)\n        return fallback[0], fallback[1], memory\n\n    memory[\"state\"] = \"APPROACH\"\n    return action, 0.90, memory\n\n\n# EVOLVE-BLOCK-END",
      "solution_sha256": "62735a598543d9d7d0b7a823e914f45b6476a626e3f1ab26a043649f006965af"
    }
  ]
}
