{
  "candidates": [
    {
      "id": "f72a3a2c-a359-43ac-819b-5de47d92ea4e",
      "iteration_found": 1,
      "solution": "# EVOLVE-BLOCK-START\ndef decide(observation, memory):\n    \"\"\"Run a safety-first finite-state controller with stable tracking and bounded reacquisition.\"\"\"\n    memory = dict(memory or {})\n    state = memory.get(\"state\", \"IDLE\")\n    scan_direction = memory.get(\"scan_direction\", \"SCAN_LEFT\")\n    scan_ticks = int(memory.get(\"scan_ticks\", 0))\n\n    visible = bool(observation[\"target_visible\"])\n    bearing = float(observation[\"target_bearing\"])\n    distance = observation[\"target_distance_class\"]\n    target_confidence = float(observation[\"target_confidence\"])\n    identity_confidence = float(observation[\"target_identity_confidence\"])\n    track_age = int(observation[\"track_age\"])\n    lost_steps = int(observation[\"lost_steps\"])\n    safety_confidence = float(observation[\"safety_confidence\"])\n    closing_risk = float(observation[\"closing_risk\"])\n    heading_error = abs(float(observation[\"heading_error\"]))\n\n    # Safety is an unconditional hard gate.\n    if safety_confidence < 0.60 or closing_risk >= 0.75:\n        memory.update(state=\"STOPPED\", scan_ticks=0)\n        return \"STOP\", 0.99, memory\n\n    confirmed = (\n        visible\n        and target_confidence >= 0.55\n        and identity_confidence >= 0.60\n    )\n\n    if not confirmed:\n        memory[\"state\"] = \"REACQUIRE\" if (\n            state in {\"LOCKED\", \"APPROACH\", \"VERIFY\", \"REACQUIRE\"} or lost_steps > 0\n        ) else \"SEARCH\"\n\n        # Briefly hold position after loss, then scan in each direction for\n        # two frames before switching, preventing rapid instruction flips.\n        if lost_steps <= 1:\n            memory[\"scan_ticks\"] = 0\n            return \"STOP\", 0.92, memory\n\n        scan_ticks += 1\n        if scan_ticks >= 2:\n            scan_direction = (\n                \"SCAN_RIGHT\" if scan_direction == \"SCAN_LEFT\" else \"SCAN_LEFT\"\n            )\n            scan_ticks = 0\n        memory[\"scan_direction\"] = scan_direction\n        memory[\"scan_ticks\"] = scan_ticks\n        return scan_direction, 0.74, memory\n\n    memory[\"scan_ticks\"] = 0\n\n    # Require a short confirmation period, but avoid repeatedly slowing an\n    # already stable track.\n    if track_age < 1:\n        memory[\"state\"] = \"VERIFY\"\n        return \"SLOW_DOWN\", 0.80, memory\n\n    if distance == \"near\":\n        if heading_error <= 15.0 and identity_confidence >= 0.80:\n            memory[\"state\"] = \"ARRIVED\"\n            return \"ARRIVED\", 0.96, memory\n\n        if bearing < 0 and observation[\"corridor_left\"]:\n            memory[\"state\"] = \"APPROACH\"\n            return \"VEER_LEFT\", 0.88, memory\n        if bearing > 0 and observation[\"corridor_right\"]:\n            memory[\"state\"] = \"APPROACH\"\n            return \"VEER_RIGHT\", 0.88, memory\n        memory[\"state\"] = \"STOPPED\"\n        return \"STOP\", 0.96, memory\n\n    memory[\"state\"] = \"APPROACH\"\n\n    # Use a modest deadband to favor direct progress while correcting only\n    # meaningful lateral error.\n    if bearing < -14.0 and observation[\"corridor_left\"]:\n        return \"VEER_LEFT\", 0.89, memory\n    if bearing > 14.0 and observation[\"corridor_right\"]:\n        return \"VEER_RIGHT\", 0.89, memory\n    if observation[\"corridor_center\"]:\n        return \"FORWARD\", 0.90, memory\n    if bearing < 0 and observation[\"corridor_left\"]:\n        return \"VEER_LEFT\", 0.80, memory\n    if bearing > 0 and observation[\"corridor_right\"]:\n        return \"VEER_RIGHT\", 0.80, memory\n\n    memory[\"state\"] = \"STOPPED\"\n    return \"STOP\", 0.98, memory\n# EVOLVE-BLOCK-END",
      "solution_sha256": "a737cf6242411f291503baed95daf4c18dc63cda81aa07db68eebba9ddac5d6c"
    }
  ]
}
