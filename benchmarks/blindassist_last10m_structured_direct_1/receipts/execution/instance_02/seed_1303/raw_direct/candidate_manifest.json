{
  "candidates": [
    {
      "id": "7ebd62e5-d953-4eea-82a9-9be22ee503a1",
      "iteration_found": 1,
      "solution": "\"\"\"L10M-ORACLE-3 conservative closed-loop candidate policy.\"\"\"\n\n\n# EVOLVE-BLOCK-START\ndef decide(observation, memory):\n    \"\"\"Apply safety gating, confidence hysteresis, bounded reacquisition, and stable target steering.\"\"\"\n    memory = dict(memory or {})\n    state = memory.get(\"state\", \"IDLE\")\n    scan_direction = memory.get(\"scan_direction\", \"SCAN_LEFT\")\n    scan_steps = int(memory.get(\"scan_steps\", 0))\n    last_action = memory.get(\"last_action\", \"\")\n\n    visible = bool(observation[\"target_visible\"])\n    bearing = float(observation[\"target_bearing\"])\n    distance = observation[\"target_distance_class\"]\n    target_confidence = float(observation[\"target_confidence\"])\n    identity_confidence = float(observation[\"target_identity_confidence\"])\n    track_age = int(observation[\"track_age\"])\n    lost_steps = int(observation[\"lost_steps\"])\n    safety_confidence = float(observation[\"safety_confidence\"])\n    closing_risk = float(observation[\"closing_risk\"])\n    heading_error = abs(float(observation[\"heading_error\"]))\n\n    def emit(action, confidence, next_state):\n        memory[\"state\"] = next_state\n        memory[\"last_action\"] = action\n        return action, confidence, memory\n\n    # Safety is an unconditional hard gate.\n    if safety_confidence < 0.60 or closing_risk >= 0.75:\n        return emit(\"STOP\", 0.99, \"STOPPED\")\n\n    trusted = visible and target_confidence >= 0.55 and identity_confidence >= 0.60\n    if not trusted:\n        if state in {\"LOCKED\", \"APPROACH\", \"VERIFY\", \"REACQUIRE\"} or lost_steps > 0:\n            if lost_steps <= 1:\n                return emit(\"STOP\", 0.92, \"REACQUIRE\")\n            scan_steps += 1\n            if scan_steps >= 2:\n                scan_direction = (\n                    \"SCAN_RIGHT\" if scan_direction == \"SCAN_LEFT\" else \"SCAN_LEFT\"\n                )\n                scan_steps = 0\n            memory[\"scan_direction\"] = scan_direction\n            memory[\"scan_steps\"] = scan_steps\n            return emit(scan_direction, 0.75, \"SEARCH\")\n        return emit(scan_direction, 0.72, \"SEARCH\")\n\n    memory[\"scan_steps\"] = 0\n    memory[\"scan_direction\"] = scan_direction\n\n    # Confirm a newly acquired track once, then retain the lock.\n    if track_age < 2 and state not in {\"LOCKED\", \"APPROACH\"}:\n        return emit(\"SLOW_DOWN\", 0.80, \"VERIFY\")\n\n    if distance == \"near\":\n        if heading_error <= 15.0 and identity_confidence >= 0.80:\n            return emit(\"ARRIVED\", 0.96, \"ARRIVED\")\n        if bearing < 0 and observation[\"corridor_left\"]:\n            return emit(\"VEER_LEFT\", 0.88, \"APPROACH\")\n        if bearing >= 0 and observation[\"corridor_right\"]:\n            return emit(\"VEER_RIGHT\", 0.88, \"APPROACH\")\n        return emit(\"STOP\", 0.94, \"STOPPED\")\n\n    # A deadband and turn hysteresis suppress needless left/right oscillation.\n    turn_threshold = 15.0\n    if abs(bearing) <= 8.0 and observation[\"corridor_center\"]:\n        return emit(\"FORWARD\", 0.90, \"APPROACH\")\n\n    if bearing < -turn_threshold and observation[\"corridor_left\"]:\n        return emit(\"VEER_LEFT\", 0.90, \"APPROACH\")\n    if bearing > turn_threshold and observation[\"corridor_right\"]:\n        return emit(\"VEER_RIGHT\", 0.90, \"APPROACH\")\n\n    if last_action == \"VEER_LEFT\" and bearing < 18.0 and observation[\"corridor_left\"]:\n        return emit(\"VEER_LEFT\", 0.84, \"APPROACH\")\n    if last_action == \"VEER_RIGHT\" and bearing > -18.0 and observation[\"corridor_right\"]:\n        return emit(\"VEER_RIGHT\", 0.84, \"APPROACH\")\n\n    if observation[\"corridor_center\"]:\n        return emit(\"FORWARD\", 0.88, \"APPROACH\")\n    if bearing < 0 and observation[\"corridor_left\"]:\n        return emit(\"VEER_LEFT\", 0.80, \"APPROACH\")\n    if bearing >= 0 and observation[\"corridor_right\"]:\n        return emit(\"VEER_RIGHT\", 0.80, \"APPROACH\")\n    return emit(\"STOP\", 0.98, \"STOPPED\")\n\n\n# EVOLVE-BLOCK-END",
      "solution_sha256": "d60000795ffca1c40fe7db8e254fe15afd08fcaef1b991bcd8a418d7f81bde35"
    }
  ]
}
