{
  "candidates": [
    {
      "id": "cf5339a7-2eaf-4ce3-ba98-02ede88a734a",
      "iteration_found": 1,
      "solution": "\"\"\"Safe temporal candidate policy for target-directed motion.\"\"\"\n\n# EVOLVE-BLOCK-START\nMOTION_ACTIONS = (\"FORWARD\", \"VEER_LEFT\", \"VEER_RIGHT\")\n\n\ndef safety_contract(observation, memory):\n    \"\"\"Stop whenever confidence is low or collision risk is high.\"\"\"\n    if observation[\"safety_confidence\"] < 0.60 or observation[\"closing_risk\"] >= 0.75:\n        memory[\"state\"] = \"STOPPED\"\n        return \"STOP\", 0.99\n    return None\n\n\ndef tracking_contract(observation, memory):\n    \"\"\"Verify identity, briefly stabilize new tracks, and scan persistently when lost.\"\"\"\n    reliable = (\n        bool(observation[\"target_visible\"])\n        and observation[\"target_confidence\"] >= 0.55\n        and observation[\"target_identity_confidence\"] >= 0.60\n    )\n    if not reliable:\n        lost = int(observation[\"lost_steps\"])\n        prior = memory.get(\"state\")\n        if prior in {\"LOCKED\", \"APPROACH\", \"REACQUIRE\"} and lost <= 1:\n            memory[\"state\"] = \"REACQUIRE\"\n            return \"STOP\", 0.92\n        direction = memory.get(\"scan_direction\", \"SCAN_LEFT\")\n        if prior == \"SEARCH\" and lost >= 3:\n            direction = \"SCAN_RIGHT\" if direction == \"SCAN_LEFT\" else \"SCAN_LEFT\"\n        memory[\"scan_direction\"] = direction\n        memory[\"state\"] = \"SEARCH\"\n        return direction, 0.75\n    if int(observation[\"track_age\"]) < 2:\n        memory[\"state\"] = \"VERIFY\"\n        return \"SLOW_DOWN\", 0.80\n    return None\n\n\ndef propose_moves(observation, memory):\n    \"\"\"Build a deduplicated set of corridor-compatible movement proposals.\"\"\"\n    bearing = float(observation[\"target_bearing\"])\n    moves = []\n    if bearing < -10.0 and observation[\"corridor_left\"]:\n        moves.append(\"VEER_LEFT\")\n    elif bearing > 10.0 and observation[\"corridor_right\"]:\n        moves.append(\"VEER_RIGHT\")\n    if observation[\"corridor_center\"]:\n        moves.append(\"FORWARD\")\n    if observation[\"corridor_left\"]:\n        moves.append(\"VEER_LEFT\")\n    if observation[\"corridor_right\"]:\n        moves.append(\"VEER_RIGHT\")\n    return list(dict.fromkeys(moves))\n\n\ndef progress_contract(observation, memory, candidates):\n    \"\"\"Use heading hysteresis and forward preference to reduce oscillation and path length.\"\"\"\n    if not candidates:\n        return None\n    error = float(observation[\"heading_error\"])\n    previous = memory.get(\"last_action\")\n    if abs(error) <= 8.0 and \"FORWARD\" in candidates:\n        action = \"FORWARD\"\n    elif error < -8.0 and \"VEER_LEFT\" in candidates:\n        action = \"VEER_LEFT\"\n    elif error > 8.0 and \"VEER_RIGHT\" in candidates:\n        action = \"VEER_RIGHT\"\n    elif previous in candidates:\n        action = previous\n    else:\n        action = candidates[0]\n    memory[\"last_action\"] = action\n    memory[\"last_heading_error\"] = error\n    return action\n\n\ndef termination_contract(observation, memory):\n    \"\"\"Return a high-confidence stationary fallback when no safe move is available.\"\"\"\n    memory[\"state\"] = \"STOPPED\"\n    return \"STOP\", 0.98\n\n\ndef decide(observation, memory):\n    \"\"\"Compose safety, tracking, arrival, progress, and termination into three outputs.\"\"\"\n    memory = dict(memory or {})\n\n    result = safety_contract(observation, memory)\n    if result is not None:\n        return result[0], result[1], memory\n\n    result = tracking_contract(observation, memory)\n    if result is not None:\n        return result[0], result[1], memory\n\n    memory[\"state\"] = \"LOCKED\"\n    near = observation[\"target_distance_class\"] == \"near\"\n    if near and (\n        abs(float(observation[\"heading_error\"])) <= 15.0\n        and observation[\"target_identity_confidence\"] >= 0.80\n    ):\n        memory[\"state\"] = \"ARRIVED\"\n        return \"ARRIVED\", 0.96, memory\n\n    if near:\n        bearing = float(observation[\"target_bearing\"])\n        preferred = \"VEER_LEFT\" if bearing < 0.0 else \"VEER_RIGHT\"\n        candidates = [preferred] if preferred in propose_moves(observation, memory) else []\n    else:\n        candidates = propose_moves(observation, memory)\n\n    action = progress_contract(observation, memory, candidates)\n    if action is None:\n        fallback = termination_contract(observation, memory)\n        return fallback[0], fallback[1], memory\n\n    memory[\"state\"] = \"APPROACH\"\n    return action, 0.90, memory\n\n\n# EVOLVE-BLOCK-END",
      "solution_sha256": "43773b79f96d3c75c8000e2240155fc674814d6a40aae2121eeceb3cdb00632e"
    }
  ]
}
