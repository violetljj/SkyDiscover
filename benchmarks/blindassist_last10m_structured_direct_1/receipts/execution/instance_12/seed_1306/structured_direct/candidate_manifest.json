{
  "candidates": [
    {
      "id": "08100ac7-e84c-43c0-8428-28354d9485b7",
      "iteration_found": 1,
      "solution": "\"\"\"Temporal candidate controller with safety gating, stable reacquisition,\ndirection-aware progress selection, and bounded action memory.\"\"\"\n\n# EVOLVE-BLOCK-START\nMOTION_ACTIONS = (\"FORWARD\", \"VEER_LEFT\", \"VEER_RIGHT\")\n\n\ndef safety_contract(observation, memory):\n    \"\"\"Stop immediately when confidence is low or collision risk is elevated.\"\"\"\n    if (\n        float(observation[\"safety_confidence\"]) < 0.60\n        or float(observation[\"closing_risk\"]) >= 0.75\n    ):\n        memory[\"state\"] = \"STOPPED\"\n        memory[\"last_action\"] = \"STOP\"\n        return \"STOP\", 0.99\n    return None\n\n\ndef tracking_contract(observation, memory):\n    \"\"\"Hold briefly after a loss, then scan with a persistent bounded direction.\"\"\"\n    visible = bool(observation[\"target_visible\"])\n    reliable = (\n        visible\n        and float(observation[\"target_confidence\"]) >= 0.55\n        and float(observation[\"target_identity_confidence\"]) >= 0.60\n    )\n    if reliable:\n        memory[\"scan_age\"] = 0\n        return None\n\n    lost_steps = int(observation[\"lost_steps\"])\n    if memory.get(\"state\") in {\"LOCKED\", \"APPROACH\", \"REACQUIRE\"} and lost_steps <= 1:\n        memory[\"state\"] = \"REACQUIRE\"\n        memory[\"last_action\"] = \"STOP\"\n        return \"STOP\", 0.92\n\n    direction = memory.get(\"scan_direction\", \"SCAN_LEFT\")\n    scan_age = int(memory.get(\"scan_age\", 0)) + 1\n    if scan_age >= 2:\n        direction = \"SCAN_RIGHT\" if direction == \"SCAN_LEFT\" else \"SCAN_LEFT\"\n        scan_age = 0\n    memory[\"scan_direction\"] = direction\n    memory[\"scan_age\"] = scan_age\n    memory[\"state\"] = \"SEARCH\"\n    memory[\"last_action\"] = direction\n    return direction, 0.75\n\n\ndef propose_moves(observation, memory):\n    \"\"\"Create unique motion proposals from bearing and visible corridor openings.\"\"\"\n    bearing = float(observation[\"target_bearing\"])\n    candidates = []\n    if abs(bearing) <= 10.0 and observation[\"corridor_center\"]:\n        candidates.append(\"FORWARD\")\n    elif bearing < -10.0 and observation[\"corridor_left\"]:\n        candidates.append(\"VEER_LEFT\")\n    elif bearing > 10.0 and observation[\"corridor_right\"]:\n        candidates.append(\"VEER_RIGHT\")\n\n    if observation[\"corridor_center\"]:\n        candidates.append(\"FORWARD\")\n    if observation[\"corridor_left\"]:\n        candidates.append(\"VEER_LEFT\")\n    if observation[\"corridor_right\"]:\n        candidates.append(\"VEER_RIGHT\")\n    return list(dict.fromkeys(candidates))\n\n\ndef progress_contract(observation, memory, candidates):\n    \"\"\"Prefer alignment-preserving motion while suppressing immediate oscillation.\"\"\"\n    if not candidates:\n        return None\n\n    bearing = float(observation[\"target_bearing\"])\n    heading = float(observation[\"heading_error\"])\n    preferred = (\n        \"VEER_LEFT\" if bearing < -8.0 or heading < -10.0\n        else \"VEER_RIGHT\" if bearing > 8.0 or heading > 10.0\n        else \"FORWARD\"\n    )\n\n    last = memory.get(\"last_action\")\n    run = int(memory.get(\"action_run\", 0))\n    ordered = [preferred] + [a for a in candidates if a != preferred]\n\n    if last in ordered and run < 2:\n        ordered.remove(last)\n        ordered.insert(0, last)\n\n    action = next((a for a in ordered if a in candidates), candidates[0])\n    if action == last:\n        memory[\"action_run\"] = run + 1\n    else:\n        memory[\"action_run\"] = 1\n    memory[\"last_action\"] = action\n    return action\n\n\ndef termination_contract(observation, memory):\n    \"\"\"Return a safe stationary fallback whenever no movement is admissible.\"\"\"\n    memory[\"state\"] = \"STOPPED\"\n    memory[\"last_action\"] = \"STOP\"\n    return \"STOP\", 0.98\n\n\ndef decide(observation, memory):\n    \"\"\"Apply safety, tracking, arrival, proposal, progress, and fallback contracts.\"\"\"\n    memory = dict(memory or {})\n\n    safety = safety_contract(observation, memory)\n    if safety is not None:\n        return safety[0], safety[1], memory\n\n    tracking = tracking_contract(observation, memory)\n    if tracking is not None:\n        return tracking[0], tracking[1], memory\n\n    memory[\"state\"] = \"LOCKED\"\n    if observation[\"target_distance_class\"] == \"near\":\n        if (\n            abs(float(observation[\"heading_error\"])) <= 15.0\n            and float(observation[\"target_identity_confidence\"]) >= 0.80\n        ):\n            memory[\"state\"] = \"ARRIVED\"\n            memory[\"last_action\"] = \"ARRIVED\"\n            return \"ARRIVED\", 0.96, memory\n        candidates = [\n            \"VEER_LEFT\"\n            if float(observation[\"target_bearing\"]) < 0.0\n            else \"VEER_RIGHT\"\n        ]\n    else:\n        candidates = propose_moves(observation, memory)\n\n    action = progress_contract(observation, memory, candidates)\n    if action is None:\n        fallback = termination_contract(observation, memory)\n        return fallback[0], fallback[1], memory\n\n    memory[\"state\"] = \"APPROACH\"\n    return action, 0.90, memory\n\n\n# EVOLVE-BLOCK-END",
      "solution_sha256": "56db22a9e9d77fae89021a7b6edae42e20c464f29543adeefc6afa3fabdac87d"
    }
  ]
}
