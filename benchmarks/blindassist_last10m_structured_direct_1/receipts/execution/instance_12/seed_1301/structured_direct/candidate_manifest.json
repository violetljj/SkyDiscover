{
  "candidates": [
    {
      "id": "8dd5dbdc-8ce6-4e00-95a7-fa93aae2ae7c",
      "iteration_found": 1,
      "solution": "\"\"\"Temporal candidate policy for safe, target-directed motion.\"\"\"\n\n# EVOLVE-BLOCK-START\nMOTION_ACTIONS = (\"FORWARD\", \"VEER_LEFT\", \"VEER_RIGHT\")\n\n\ndef safety_contract(observation, memory):\n    \"\"\"Stop whenever confidence is low or collision-closing risk is high.\"\"\"\n    if (\n        float(observation[\"safety_confidence\"]) < 0.60\n        or float(observation[\"closing_risk\"]) >= 0.75\n    ):\n        memory[\"state\"] = \"STOPPED\"\n        memory[\"turn_bias\"] = None\n        return \"STOP\", 0.99\n    return None\n\n\ndef tracking_contract(observation, memory):\n    \"\"\"Require a stable identity and use bounded stopping or alternating scans after loss.\"\"\"\n    reliable = (\n        bool(observation[\"target_visible\"])\n        and float(observation[\"target_confidence\"]) >= 0.55\n        and float(observation[\"target_identity_confidence\"]) >= 0.60\n    )\n    if reliable:\n        if int(observation[\"track_age\"]) < 2:\n            memory[\"state\"] = \"VERIFY\"\n            return \"SLOW_DOWN\", 0.80\n        return None\n\n    lost_steps = int(observation[\"lost_steps\"])\n    if memory.get(\"state\") in {\"LOCKED\", \"APPROACH\", \"REACQUIRE\"} and lost_steps <= 1:\n        memory[\"state\"] = \"REACQUIRE\"\n        return \"STOP\", 0.92\n\n    direction = memory.get(\"scan_direction\", \"SCAN_LEFT\")\n    memory[\"scan_direction\"] = (\n        \"SCAN_RIGHT\" if direction == \"SCAN_LEFT\" else \"SCAN_LEFT\"\n    )\n    memory[\"state\"] = \"SEARCH\"\n    return direction, 0.75\n\n\ndef propose_moves(observation, memory):\n    \"\"\"Build corridor-valid moves with hysteretic steering to reduce needless zigzags.\"\"\"\n    bearing = float(observation[\"target_bearing\"])\n    prior = memory.get(\"turn_bias\")\n    engage = 18.0\n    release = 8.0\n\n    steer = None\n    if prior == \"VEER_LEFT\":\n        if bearing < -release:\n            steer = \"VEER_LEFT\"\n    elif prior == \"VEER_RIGHT\":\n        if bearing > release:\n            steer = \"VEER_RIGHT\"\n    elif bearing < -engage:\n        steer = \"VEER_LEFT\"\n    elif bearing > engage:\n        steer = \"VEER_RIGHT\"\n\n    candidates = []\n    if steer == \"VEER_LEFT\" and observation[\"corridor_left\"]:\n        candidates.append(\"VEER_LEFT\")\n    if steer == \"VEER_RIGHT\" and observation[\"corridor_right\"]:\n        candidates.append(\"VEER_RIGHT\")\n    if observation[\"corridor_center\"]:\n        candidates.append(\"FORWARD\")\n    if steer is None:\n        if bearing < 0.0 and observation[\"corridor_left\"]:\n            candidates.append(\"VEER_LEFT\")\n        elif bearing > 0.0 and observation[\"corridor_right\"]:\n            candidates.append(\"VEER_RIGHT\")\n    if observation[\"corridor_left\"]:\n        candidates.append(\"VEER_LEFT\")\n    if observation[\"corridor_right\"]:\n        candidates.append(\"VEER_RIGHT\")\n    return list(dict.fromkeys(candidates))\n\n\ndef progress_contract(observation, memory, candidates):\n    \"\"\"Prefer hysteretic steering, otherwise advance straight and remember the choice.\"\"\"\n    if not candidates:\n        return None\n\n    previous = memory.get(\"last_action\")\n    bearing = float(observation[\"target_bearing\"])\n    action = candidates[0]\n\n    if previous in candidates and previous in {\"VEER_LEFT\", \"VEER_RIGHT\"}:\n        same_side = (\n            previous == \"VEER_LEFT\" and bearing < -8.0\n        ) or (\n            previous == \"VEER_RIGHT\" and bearing > 8.0\n        )\n        if same_side:\n            action = previous\n    elif \"FORWARD\" in candidates and abs(bearing) <= 18.0:\n        action = \"FORWARD\"\n\n    memory[\"last_action\"] = action\n    memory[\"turn_bias\"] = action if action in {\"VEER_LEFT\", \"VEER_RIGHT\"} else None\n    return action\n\n\ndef termination_contract(observation, memory):\n    \"\"\"Return a high-confidence stationary fallback when no safe move is available.\"\"\"\n    memory[\"state\"] = \"STOPPED\"\n    memory[\"last_action\"] = \"STOP\"\n    memory[\"turn_bias\"] = None\n    return \"STOP\", 0.98\n\n\ndef decide(observation, memory):\n    \"\"\"Compose safety, tracking, progress, and termination into the fixed three-value API.\"\"\"\n    memory = dict(memory or {})\n\n    safety = safety_contract(observation, memory)\n    if safety is not None:\n        return safety[0], safety[1], memory\n\n    tracking = tracking_contract(observation, memory)\n    if tracking is not None:\n        return tracking[0], tracking[1], memory\n\n    memory[\"state\"] = \"LOCKED\"\n    if observation[\"target_distance_class\"] == \"near\":\n        if (\n            abs(float(observation[\"heading_error\"])) <= 15.0\n            and float(observation[\"target_identity_confidence\"]) >= 0.80\n        ):\n            memory[\"state\"] = \"ARRIVED\"\n            memory[\"last_action\"] = \"ARRIVED\"\n            memory[\"turn_bias\"] = None\n            return \"ARRIVED\", 0.96, memory\n        bearing = float(observation[\"target_bearing\"])\n        candidates = [\n            \"VEER_LEFT\" if bearing < 0.0 else \"VEER_RIGHT\"\n        ]\n    else:\n        candidates = propose_moves(observation, memory)\n\n    action = progress_contract(observation, memory, candidates)\n    if action is None:\n        fallback = termination_contract(observation, memory)\n        return fallback[0], fallback[1], memory\n\n    memory[\"state\"] = \"APPROACH\"\n    return action, 0.90, memory\n\n\n# EVOLVE-BLOCK-END",
      "solution_sha256": "2488b5384da7521e05496f4011fa6ceac384d0ca3efe52303c83548f05fe7ae8"
    }
  ]
}
