{
  "candidates": [
    {
      "id": "4349b3cf-16b9-40fe-ba2a-e68f42dee52d",
      "iteration_found": 1,
      "solution": "\"\"\"Temporal candidate controller preserving the six structured contracts.\"\"\"\n\n# EVOLVE-BLOCK-START\nMOTION_ACTIONS = (\"FORWARD\", \"VEER_LEFT\", \"VEER_RIGHT\")\n\n\ndef safety_contract(observation, memory):\n    \"\"\"Stop whenever safety confidence is low or collision risk is elevated.\"\"\"\n    if observation[\"safety_confidence\"] < 0.60 or observation[\"closing_risk\"] >= 0.75:\n        memory[\"state\"] = \"STOPPED\"\n        return \"STOP\", 0.99\n    return None\n\n\ndef tracking_contract(observation, memory):\n    \"\"\"Maintain a stable target lock and use bounded, persistent reacquisition scans.\"\"\"\n    visible = bool(observation[\"target_visible\"])\n    reliable = (\n        visible\n        and observation[\"target_confidence\"] >= 0.55\n        and observation[\"target_identity_confidence\"] >= 0.60\n    )\n    if reliable:\n        memory[\"lost_steps\"] = 0\n        if observation[\"track_age\"] < 2:\n            memory[\"state\"] = \"VERIFY\"\n            return \"SLOW_DOWN\", 0.80\n        return None\n\n    lost_steps = int(observation[\"lost_steps\"])\n    memory[\"lost_steps\"] = lost_steps\n    if memory.get(\"state\") in {\"LOCKED\", \"APPROACH\", \"REACQUIRE\"} and lost_steps <= 1:\n        memory[\"state\"] = \"REACQUIRE\"\n        return \"STOP\", 0.92\n\n    direction = memory.get(\"scan_direction\", \"SCAN_LEFT\")\n    if lost_steps >= 3:\n        direction = \"SCAN_RIGHT\" if direction == \"SCAN_LEFT\" else \"SCAN_LEFT\"\n        memory[\"scan_direction\"] = direction\n    memory[\"state\"] = \"SEARCH\"\n    return direction, 0.75\n\n\ndef propose_moves(observation, memory):\n    \"\"\"Generate corridor-constrained motion candidates ordered by bearing alignment.\"\"\"\n    bearing = float(observation[\"target_bearing\"])\n    candidates = []\n    if bearing < -10.0 and observation[\"corridor_left\"]:\n        candidates.append(\"VEER_LEFT\")\n    elif bearing > 10.0 and observation[\"corridor_right\"]:\n        candidates.append(\"VEER_RIGHT\")\n    if observation[\"corridor_center\"]:\n        candidates.append(\"FORWARD\")\n    if observation[\"corridor_left\"]:\n        candidates.append(\"VEER_LEFT\")\n    if observation[\"corridor_right\"]:\n        candidates.append(\"VEER_RIGHT\")\n    return list(dict.fromkeys(candidates))\n\n\ndef progress_contract(observation, memory, candidates):\n    \"\"\"Choose a directionally aligned move while damping unnecessary turn reversals.\"\"\"\n    if not candidates:\n        return None\n\n    bearing = float(observation[\"target_bearing\"])\n    last = memory.get(\"last_action\")\n    preferred = None\n    if bearing < -10.0:\n        preferred = \"VEER_LEFT\"\n    elif bearing > 10.0:\n        preferred = \"VEER_RIGHT\"\n    else:\n        preferred = \"FORWARD\"\n\n    if preferred in candidates:\n        action = preferred\n    elif \"FORWARD\" in candidates:\n        action = \"FORWARD\"\n    else:\n        action = candidates[0]\n\n    if (\n        last in {\"VEER_LEFT\", \"VEER_RIGHT\"}\n        and action in {\"VEER_LEFT\", \"VEER_RIGHT\"}\n        and action != last\n        and abs(bearing) < 18.0\n        and \"FORWARD\" in candidates\n    ):\n        action = \"FORWARD\"\n\n    memory[\"last_action\"] = action\n    memory[\"turn_count\"] = int(memory.get(\"turn_count\", 0)) + (\n        1 if action in {\"VEER_LEFT\", \"VEER_RIGHT\"} else 0\n    )\n    return action\n\n\ndef termination_contract(observation, memory):\n    \"\"\"Return a high-confidence stationary fallback when no motion is admissible.\"\"\"\n    memory[\"state\"] = \"STOPPED\"\n    return \"STOP\", 0.98\n\n\ndef decide(observation, memory):\n    \"\"\"Compose safety, tracking, approach, progress, and termination into three values.\"\"\"\n    memory = dict(memory or {})\n\n    safety = safety_contract(observation, memory)\n    if safety is not None:\n        return safety[0], safety[1], memory\n\n    tracking = tracking_contract(observation, memory)\n    if tracking is not None:\n        return tracking[0], tracking[1], memory\n\n    memory[\"state\"] = \"LOCKED\"\n    if observation[\"target_distance_class\"] == \"near\":\n        if (\n            abs(float(observation[\"heading_error\"])) <= 15.0\n            and observation[\"target_identity_confidence\"] >= 0.80\n        ):\n            memory[\"state\"] = \"ARRIVED\"\n            return \"ARRIVED\", 0.96, memory\n        candidates = [\n            \"VEER_LEFT\" if float(observation[\"target_bearing\"]) < 0.0 else \"VEER_RIGHT\"\n        ]\n    else:\n        candidates = propose_moves(observation, memory)\n\n    action = progress_contract(observation, memory, candidates)\n    if action is None:\n        fallback = termination_contract(observation, memory)\n        return fallback[0], fallback[1], memory\n\n    memory[\"state\"] = \"APPROACH\"\n    return action, 0.90, memory\n\n\n# EVOLVE-BLOCK-END",
      "solution_sha256": "df03672da67ef6690d1899f26938ceedd7cfb3ac982ea3ea7f6f2ea0c67107c2"
    }
  ]
}
