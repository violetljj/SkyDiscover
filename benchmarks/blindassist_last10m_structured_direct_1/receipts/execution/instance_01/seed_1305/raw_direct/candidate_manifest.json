{
  "candidates": [
    {
      "id": "0a91051e-a99c-4e3b-9dea-00732e68a659",
      "iteration_found": 1,
      "solution": "\"\"\"L10M-ORACLE-3 conservative closed-loop navigation policy.\"\"\"\n\n\n# EVOLVE-BLOCK-START\ndef decide(observation, memory):\n    \"\"\"Use safety gating, confidence confirmation, hysteretic reacquisition, and\n    deadband steering to approach only a currently verified target.\n    \"\"\"\n    memory = dict(memory or {})\n    state = memory.get(\"state\", \"IDLE\")\n    scan_direction = memory.get(\"scan_direction\", \"SCAN_LEFT\")\n    last_action = memory.get(\"last_action\", \"\")\n    lost_steps = int(observation[\"lost_steps\"])\n    visible = bool(observation[\"target_visible\"])\n    target_confidence = float(observation[\"target_confidence\"])\n    identity_confidence = float(observation[\"target_identity_confidence\"])\n    safety_confidence = float(observation[\"safety_confidence\"])\n    closing_risk = float(observation[\"closing_risk\"])\n    bearing = float(observation[\"target_bearing\"])\n    heading_error = abs(float(observation[\"heading_error\"]))\n    track_age = int(observation[\"track_age\"])\n    distance = observation[\"target_distance_class\"]\n\n    def emit(action, confidence, next_state):\n        memory[\"state\"] = next_state\n        memory[\"last_action\"] = action\n        return action, confidence, memory\n\n    if safety_confidence < 0.60 or closing_risk >= 0.75:\n        return emit(\"STOP\", 0.99, \"STOPPED\")\n\n    confirmed = (\n        visible\n        and target_confidence >= 0.55\n        and identity_confidence >= 0.60\n    )\n    if not confirmed:\n        if state in {\"LOCKED\", \"APPROACH\", \"VERIFY\", \"REACQUIRE\"} or lost_steps:\n            if lost_steps <= 1:\n                return emit(\"STOP\", 0.92, \"REACQUIRE\")\n            direction = scan_direction\n            memory[\"scan_direction\"] = (\n                \"SCAN_RIGHT\" if direction == \"SCAN_LEFT\" else \"SCAN_LEFT\"\n            )\n            return emit(direction, 0.76, \"REACQUIRE\")\n        return emit(scan_direction, 0.74, \"SEARCH\")\n\n    if track_age < 2:\n        return emit(\"SLOW_DOWN\", 0.80, \"VERIFY\")\n\n    if distance == \"near\":\n        if heading_error <= 15.0 and identity_confidence >= 0.80:\n            return emit(\"ARRIVED\", 0.96, \"ARRIVED\")\n        if abs(bearing) <= 8.0 and observation[\"corridor_center\"]:\n            return emit(\"SLOW_DOWN\", 0.84, \"APPROACH\")\n    else:\n        if abs(bearing) <= 10.0 and observation[\"corridor_center\"]:\n            return emit(\"FORWARD\", 0.91, \"APPROACH\")\n\n    if bearing < -12.0 and observation[\"corridor_left\"]:\n        return emit(\"VEER_LEFT\", 0.89, \"APPROACH\")\n    if bearing > 12.0 and observation[\"corridor_right\"]:\n        return emit(\"VEER_RIGHT\", 0.89, \"APPROACH\")\n\n    if abs(bearing) <= 12.0:\n        if observation[\"corridor_center\"]:\n            return emit(\"FORWARD\", 0.90, \"APPROACH\")\n        if last_action == \"VEER_LEFT\" and observation[\"corridor_left\"]:\n            return emit(\"VEER_LEFT\", 0.83, \"APPROACH\")\n        if last_action == \"VEER_RIGHT\" and observation[\"corridor_right\"]:\n            return emit(\"VEER_RIGHT\", 0.83, \"APPROACH\")\n\n    if observation[\"corridor_left\"]:\n        return emit(\"VEER_LEFT\", 0.82, \"APPROACH\")\n    if observation[\"corridor_right\"]:\n        return emit(\"VEER_RIGHT\", 0.82, \"APPROACH\")\n    return emit(\"STOP\", 0.98, \"STOPPED\")\n# EVOLVE-BLOCK-END",
      "solution_sha256": "56aec9c3a1a49fefcb668b42755da07dbefd521f1d8387fe31f7129d55828b63"
    }
  ]
}
