"""SkyDiscover-Synthesize: coding agents build a system to your spec, checked by tests or proofs.

workflow/ is the /skysynth skill, spec/ the Python the agents call, examples/ the checked-in
domains, scripts/ the installer.
"""
