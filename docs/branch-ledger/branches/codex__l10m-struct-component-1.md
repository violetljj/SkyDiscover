# `codex/l10m-struct-component-1`

- Status: `ready_for_consumed_dev_execution_no_arm_runs`
- Owner: project work branch
- Base: `codex/l10m-struct-autopsy-1` at `c189e13`
- Integration target: `main`, after the full L10M dependency chain
- Integrated commit: `not integrated`

## Purpose

Implement `L10M-STRUCT-COMPONENT-1`, a consumed/development 2x2 factorial
screen isolating progress memory and explicit move proposals. The sealed
`L10M-STRUCT-AUTOPSY-1` node remains read-only.

## Evidence boundary

- Uses the already consumed `L10M-COMP-2` 12-instance cohort.
- Has no fresh or blind claim authority.
- Freezes design, code, guard, recovery, and hashes before any formal arm run.
- Does not modify any prior evaluator, cohort, receipt, manifest, or report.

## Isolation contract

All arms share one byte-identical initial scaffold and one config. Candidate
source outside the arm-admitted component bodies must remain byte-identical.
Safety, tracking, termination, and composition are immutable. Any violation is
an ITT zero before evaluator import. A common inert comment tag is the only
non-component candidate difference and prevents raw-control deduplication.

## Claim ceiling

Only a consumed-development mechanism-screen signal within this L10M graph
family is admissible. The screen may open a separately preregistered fresh
confirmation gate; it cannot establish component superiority or reinterpret
the Structured Direct result.

## Validation state

- Mechanical preflight passed with zero model calls and zero formal arm runs.
- All 24 consumed cohort files matched the frozen source manifest.
- Four valid arm examples passed the guard and a locked-safety mutation failed.
- The execution manifest binds the final protocol, scaffold, prompts, guard,
  evaluator, harness, analysis, progress summarizer, cohort, and shared runtime.
